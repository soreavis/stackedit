# Changelog

All notable changes to this fork of [`benweet/stackedit`](https://github.com/benweet/stackedit) are documented here.

This project follows [Keep a Changelog](https://keepachangelog.com/) and [Semantic Versioning](https://semver.org/). Version identifiers use the `x.y.z-fork.N` suffix so upstream semver stays distinguishable.

## [Unreleased]

### Changed
- **Drop `@vercel/node` devDep + tighten core types** — replaces `@vercel/node`'s `VercelRequest` / `VercelResponse` type imports with a 19-line local declaration in `api/_types.ts`. The package was a devDep used only for type imports but pulled in `path-to-regexp`, `undici`, `ajv`, `smol-toml`, and `minimatch` — all flagged by `npm audit` for ReDoS / DoS issues. **17 → 10 npm-audit vulnerabilities** (the 10 remaining are all build-time / dev-time: vue 2.7's parseHTML ReDoS — unfixable until Vue 3, vue-template-compiler XSS in build pipeline, vite-plugin-pwa → serialize-javascript, mermaid → uuid v3-only-vuln false positive). Then tightens `: any` in three core hot-path service files: `utils.ts` (36 → 0), `diffUtils.ts` (19 → 0), `syncSvc.ts` (67 → 51, focused on the wire-shape boundary). New types: `QueryParams`, `Item`, `Constants`, `Crypto` typed without IE-era casts; `deepCopy<T>(obj: T): T` generic; `search<T>` generic; `serializeObject(unknown): string | undefined`; `getItemHash(Item): number`; `addItemHash<T>(T): T & { hash: number }`. `diffUtils` gains `DiffMatchPatchInstance` interface, `DiscussionLike`, `ContentLike`, `MarkerKey` / `Marker` / `MarkerIdxMap`, generic `mergeValues<T>`. `syncSvc` exports its core wire shapes (`SyncData`, `Change`, `ChangeItem`, `SyncedContent`) at module top; `applyChanges`, `upgradeSyncedContent`, `cleanSyncedContent` use them throughout. The provider response shapes stay `any` (heterogeneous vendor APIs) and cross-store reads keep `(useXStore() as any)` casts since not every store is fully typed yet. No runtime change. Lint 0/0, typecheck clean, unit 479/479, build clean, main bundle 138.9 kB brotli (unchanged). Live boot smoke test green.
- **`@ts-nocheck` removal — 6 heavyweight services (6 → 0). All `src/` files are now strictly type-checked.** Strips `@ts-nocheck` from `utils.ts` (368 lines, low-level helpers — yaml, marker offsets, hashing), `diffUtils.ts` (204 lines, diff-match-patch wrapper), `optional/scrollSync.ts` (185 lines, animationSvc fluent chain), `editor/editorSvcDiscussions.ts` (294 lines, markers + class-applier pipeline), `editorSvc.ts` (585 lines, Object.assign mixin shape), `syncSvc.ts` (976 lines, workspace sync orchestrator). Strategy: same pragmatic approach as the providers batch — `: any` annotations on params/returns, `(svc as any).method()` casts at boundaries, explicit types on module-level lets. `editorSvc.ts` uses `this: any` on every method to escape the literal-object inference that doesn't see Object.assign-mixed-in members. `markdown-it-pandoc-renderer` import gets a single `@ts-expect-error` since the package ships no types. The animationSvc fluent chain (`animate(el).scrollTop(N).duration(D).start(cb1, cb2)`) is wrapped via `((animationSvc as any).animate(el) as any).scrollTop(...)` at each call site. **0 `@ts-nocheck` files remain under `src/`** (down from 33 at the start of this modernization). Lint 0/0, typecheck clean, unit 479/479, build clean, main bundle 138.9 kB brotli (unchanged). Live boot smoke test green.
- **`@ts-nocheck` removal — 16 provider subclasses + 7 provider helpers (27 → 6)** — strips `@ts-nocheck` from `bloggerProvider`, `bloggerPageProvider`, `couchdbWorkspaceProvider`, `dropboxProvider`, `gistProvider`, `githubProvider`, `githubWorkspaceProvider`, `gitlabProvider`, `gitlabWorkspaceProvider`, `googleDriveProvider`, `googleDriveAppDataProvider`, `googleDriveWorkspaceProvider`, `wordpressProvider`, `zendeskProvider`, plus all 7 helpers (`couchdbHelper`, `dropboxHelper`, `githubHelper`, `gitlabHelper`, `googleHelper`, `wordpressHelper`, `zendeskHelper`). Strategy: pragmatic minimal typing — method signatures get `: any` for params and `Promise<any>` / `any` for returns, cross-module references cast via `(googleHelper as any).foo()` / `(utils as any).bar()` / etc., `(this as any).id` in Provider method shorthand. Designing per-provider response shapes (Drive v3, GitHub v3, GitLab v4, WordPress.com REST, Zendesk Help Center, Dropbox v2, CouchDB) would take weeks each — out of scope. Module-level let-bindings (`fileIdToOpen`, `syncStartPageToken`, `savedSha`) gain explicit types. Missing `useFileStore` imports added where needed. **All 21 files in `src/services/providers/`** are now strictly type-checked. 6 `@ts-nocheck` files remain: `syncSvc`, `utils`, `diffUtils`, `editorSvc`, `editorSvcDiscussions`, `optional/scrollSync` — all heavyweight services that need their own focused batches. Lint 0/0, typecheck clean, unit 479/479, build clean, main bundle 138.9 kB brotli (unchanged). Live boot smoke test green.
- **`@ts-nocheck` removal — `optional/shortcuts.ts` (28 → 27)** — the keyboard shortcut + text-expansion glue gets typed: `pagedownHandler(name: string)`, `findReplaceOpener(type: 'find' | 'replace')`, typed `modifierMap` / `namedKeyMap`, typed `toTinykeys(chord: string): string | null`, typed `guard(fn): (event: KeyboardEvent) => void`, `Shortcut` / `Expansion` interfaces. The dynamic editorSvc surface (`.clEditor`, `.$on`) keeps narrow `as any` casts at the boundary — full editorSvc typing is its own batch (it's built via Object.assign mixins). Lint 0/0, typecheck clean, unit 479/479, build clean, main bundle 138.9 kB brotli (unchanged). Live boot smoke test green.
- **`@ts-nocheck` removal — provider base + registry (30 → 28)** — `src/services/providers/common/providerRegistry.ts` (12 lines, no logic — just a typed `register` generic and a `providersById: Record<string, ProviderShape>` map) and `src/services/providers/common/Provider.ts` (the base class for all 16 sync/publish provider subclasses) shed `@ts-nocheck`. `Provider` gets typed `serializeContent` / `parseContent` / `openFileWithLocation` static methods (with `SerializedContent` and `ParsedContent` interfaces local to the file), typed instance fields (`prepareChanges`, `onChangesApplied`, `id`), and a typed constructor. The two helpers cast `utils.*` and `workspaceSvc.*` calls to permissive function types since those modules still carry `@ts-nocheck`. The `emptyContent(id)` calls cast to `(id: string) => ParsedContent` because the JS empty factory infers as `(id?: null) => ...` from its `id = null` default. The 16 provider subclasses (Dropbox / Google / GitHub / GitLab / WordPress / Zendesk / Blogger / etc.) still carry `@ts-nocheck` — typing them needs per-provider response shape design that's out of scope for a single batch. Lint 0/0, typecheck clean, unit 479/479, build clean, main bundle 138.9 kB brotli (unchanged). Live boot smoke test green.
- **ESLint 9 → 10** — bumps `eslint` from 9.39.4 to 10.2.1 and `@eslint/js` from 9.39.4 to 10.0.1. ESLint 10 added a `scopeManager.addGlobals` call in `addDeclaredGlobals` (`source-code.js`) that throws when the configured parser produces a scope manager without that method. `@babel/eslint-parser`'s peer-dep range is `^7 || ^8 || ^9` — it doesn't yet support ESLint 10. Fixed by dropping `@babel/eslint-parser` entirely (uninstalled) and removing the `parserOptions.parser: babelParser` line from `eslint.config.mjs`. `vue-eslint-parser@^10.4.0` (already installed; peer is `^8.57.0 || ^9 || ^10`) now uses its default inner parser (espree) for the `<script>` blocks of `.vue` files and for plain `.js` files. The codebase doesn't use any non-standard JS features (decorators, type-only imports in JS) that would need babel — espree handles `ecmaVersion: 'latest'`, `sourceType: 'module'` cleanly. Lint **0/0**, typecheck clean, unit 479/479, build clean, main bundle 138.9 kB brotli (unchanged). Live boot smoke test green.
- **Stores → TypeScript, batch 3: 5 complex stores** — converts the final 5 stores from `.js` to `.ts`: `workspace`, `discussion`, `content`, `explorer`, `data`. **All 22 stores in `src/stores/` are now `.ts`.** Each store gets typed `State` interfaces, typed action parameters, typed getter return values. The `Node` class in `explorer.ts` implements an `ExplorerNodeShape` interface; the existing `Node` constructor receives `emptyFile()` / `emptyFolder()` results cast through `unknown as ExplorerItem` since the JS empties return `id: null` while the typed `ExplorerItem` requires `string`. The `data` store keeps action signatures permissive (`unknown` for the various heterogeneous payloads it stores) — typing each branch precisely would require designing token / settings / template shapes globally, out of scope for this batch. Cross-store reads still cast to `as any` at the call site since not every consumer of these stores is typed yet. Service-side fixes: `localDbSvc.ts` casts `welcomeFileHashes` to `Record<string, number>` (previously implicit any from data store), `gitWorkspaceSvc.ts` and `localDbSvc.ts:45` cast `currentWorkspace.id`/`.path` to `string` (Workspace interface keeps them optional). `explorerSvc.ts` casts the explorer-store getters to its local `ExplorerNode` shape via `as unknown as ExplorerNode[]` (the local interface declares `folders`/`files` as required while the new shape leaves them optional — a relaxation the new typed store can't satisfy without changing runtime behavior). `uiPersistence.ts` casts the JSON-parsed `setOpenNodes` arg. No runtime change. Lint 0/0, typecheck clean, unit 479/479, build clean, main bundle 138.9 kB brotli (limit 200; unchanged). Live boot smoke test: editor mounts, file tree renders, zero console errors.
- **Stores → TypeScript, batch 2: item-store factory + 7 item stores** — converts `itemStoreFactory`, `locationStoreFactory`, and the 7 stores that ride on them (`folder`, `syncedContent`, `contentState`, `userInfo`, `file`, `syncLocation`, `publishLocation`, plus `itemBridge`) from `.js` to `.ts`. Strategy: store-internal types are strict (`BaseItem.id: string`, `setItem(value: Partial<T> & { id: string })`, `patchItem(...)`), the dirty-data flow at the consumer boundary gets contained casts. The `data/empties/empty*.js` factories stay JS — each store imports its `empty` raw and casts it once at the factory call site (`emptyFolderRaw as unknown as (id?: string) => Folder`). LocationItem keeps `fileId` / `providerId` optional to match the runtime placeholder shape; `groupedByFileId` / `groupedByFileIdAndHash` getters skip items where `fileId` isn't set. `userInfo` defines a strict `UserInfo` interface (no index signature) so callers passing local `UserInfo` types match nominally. Service-side fixes: `workspaceSvc.ts` casts `item.type as string` at three `setItemByType` / `patchItemByType` / `deleteItemByType` call sites (item.type is always present at those sites, but the local `Item` interface keeps it optional); `publishSvc.ts` casts `publishLocationToStore as PublishLocation & { id: string }` at the one `patchItem` call where the publishLocation comes back from the provider without a typed id. No runtime change. Lint 0/0, typecheck clean, unit 479/479, build clean, main bundle 138.91 kB brotli (limit 200; +0.12 kB from type annotations). Live boot smoke test: editor mounts, file tree renders, zero console errors.
- **Stores → TypeScript, batch 1: simple state stores** — converts `findReplace`, `contextMenu`, `queue`, `notification`, `modal`, `global`, `layout` from `.js` to `.ts`. Each gets a typed `State` interface, typed action parameters, typed getter return values. Cross-store reads (`useDataStore` / `useFileStore` / `useDiscussionStore` / etc.) cast at the call site (`as unknown as { ... }`) since those stores haven't been ported yet. The `pagedownButtons` data file is typed inline with a local `PagedownButton` interface. Pinia's `defineStore` infers the rest. No runtime change. Lint 0/0, typecheck clean, unit 479/479, build clean, main bundle 138.79 kB brotli (limit 200; unchanged — TS files compile to similar-size JS).

### Added
- **YAML syntax highlighting restored in CodeEditor** — installs `@codemirror/lang-yaml`. `cm6SmallEditor` gains a `'yaml'` language option that wires the lang extension into the small-editor extension list. `CodeEditor.vue` maps its `lang` prop (e.g. `'yaml'`, `'handlebars'`) to the matching CM6 language ('yaml' / 'markdown' / 'plain'). Closes a feature regression from PR #130, which dropped Prism's per-language syntax highlighting when porting CodeEditor to CM6. Lint 0/0, typecheck clean, unit 479/479, build clean, main bundle 138.89 kB brotli (limit 200; +0.3 kB from the lang import — negligible since it lands in the lazy CM6 chunk).

### Changed
- **`@ts-nocheck` removal — 3 files cleaned (33 → 30)** — `src/services/optional/index.ts` (header-only barrel — no actual types to add), `src/services/optional/taskChange.ts` (added `editorSvcRaw as any` cast at the import boundary, typed the DOM event target + `getPreviewOffset` recursion), and `src/services/templateWorker.ts` (added a `TocItem` interface for the `tocToHtml` Handlebars helper, typed the `safeEval(code: string)` parameter, single `(self as any).Handlebars` cast for the worker-global assignment). Other candidates — `optional/scrollSync.ts`, `optional/shortcuts.ts`, `editorSvc.ts`, `editorSvcDiscussions.ts`, the provider files, `diffUtils.ts`, `utils.ts` — keep `@ts-nocheck` because they reach into animationSvc's fluent chain API, the editorSvc Object.assign mixin shape, or external API responses (Google / GitHub / Dropbox / etc.) that need real type design beyond a single-file edit. Tracked as a long-tail follow-up.
- **Lint cleanup — 17 → 0 warnings** — swept the leftover unused-import warnings (mostly `mapPiniaState` / `mapPiniaActions` / `mapState` aliases imported from `'pinia'` but never read) across 15 components. Each import statement gets the unused name surgically dropped (kept the rest of the destructure intact). Lint now reports **0 errors / 0 warnings**. No runtime change. Typecheck clean, unit 479/479, build clean, main bundle 138.59 kB brotli (unchanged — these were dead imports).

### Removed
- **`src/services/editor/cledit/` deleted (~1800 lines, 7 files)** — closes out Stage 3. The two niche editors that were the last cledit consumers — `CodeEditor.vue` (settings YAML / template editor) and `gutters/NewComment.vue` (discussion comment input) — are now ported to a new lightweight `mountSmallEditor` builder under `src/services/editor/cm6/cm6SmallEditor.ts`. The new builder skips the main editor's heavier extensions (line numbers, active-line highlight, decoration fields, marker fields) — those would look out of place in a comment-box / settings-textarea — but keeps history, drawSelection, lineWrapping, the StackEdit highlight style, and the markdown / default / history / indentWithTab keymaps. Returns a small handle with `getContent / setContent / focus / toggleEditable / on('contentChanged') / on('focus') / selectionMgr.{selectionStart, selectionEnd, setSelectionStartEnd, on('selectionChanged')} / destroy`. Both consumers dynamic-import the builder so the chunk stays out of the main bundle. CodeEditor's per-language Prism syntax highlighting is dropped — settings YAML now renders as plain monospace; adding `@codemirror/lang-yaml` is a follow-up if the colors are missed. **Main bundle dropped 6.24 kB brotli (144.83 → 138.59 kB)** from removing the cledit static-import chain. Lint 0 errors / 17 warnings (unchanged), typecheck clean, unit 479/479, build clean. Live verified: main editor mounts cleanly + Settings modal renders the CodeEditor's CM6 instance.

### Fixed
- **List auto-renumbering on Tab / Shift+Tab — restored under CM6** — port of the cledit `keystrokes.ts fixNumberedList` logic to a CM6 keymap entry. New `src/services/editor/cm6/cm6ListRenumber.ts` exposes `findOrderedRun(view, lineNum)` (walks outward to find the contiguous block of ordered-list lines at the same indent — sweeping past more-indented sub-list lines as continuation, breaking on blank / different-indent / non-list content), `buildRenumberChanges(view, run)` (rewrites just the `<digits>` portion of every list line in the run, preserving the run's existing first-item number), and `tabRenumberCommand` / `shiftTabRenumberCommand` keymap entries that call `indentMore` / `indentLess` then renumber the affected runs in a follow-up transaction. Wired into the bridge's keymap before `indentWithTab` so the new commands handle ordered-list lines and fall through (return false) elsewhere. Runs scoped to same-indent contiguous blocks (more cautious than cledit which used a more permissive regex; less risk of mis-renumbering edge cases). 13 new unit tests cover findOrderedRun (single line, contiguous, indent change, sub-list jump, non-list break), buildRenumberChanges (no-op, sequence reset, multi-digit, custom start), tabRenumberCommand (no-op outside list, indent + outer-run renumber when sub-list inserted), shiftTabRenumberCommand (dedent + outer renumber). Test count 466 → 479. Lint 0 errors / 17 warnings, typecheck clean, build clean, main bundle 144.83 kB brotli. Live: typing `1. one`+Enter+`two`+Enter+`three` gets auto-numbered by markdownKeymap (`1. one / 2. two / 3. three`); pressing Tab on line 2 indents it AND renumbers line 3 from `3. three` to `2. three`.
- **Text-expansion sequences (`==> ` → `⇒ `, `<== ` → `⇐ `) — restored on the CM6 bridge** — `optional/shortcuts.ts applyExpansion` was using `selectionMgr.createRange().deleteContents() / insertNode(textNode)` which only worked against cledit's DOM-Range-backed `selectionMgr`. The CM6 bridge's `createRange` returns a plain `{ from, to }` object — those Range methods don't exist, so the trigger replacement silently no-op'd. Rewrote to read the doc text via `editor.getContent()`, compare the trailing N chars to each trigger, and call `editor.replace(start, end, replacement)` (which the bridge implements via a CM6 transaction with `selection: EditorSelection.cursor(...)` so the cursor lands at the end of the replacement). Also corrected the listener — the previous `editorSvc.$on('contentChanged', ...)` was bound to a non-existent event (editorSvc emits `sectionList`, `selectionRange`, `previewCtx`, etc., never `contentChanged`); switched to `sectionList` which fires on every meaningful edit. The wrong-listener bug existed in cledit mode too — text-expansion was probably broken for a long time, just less visibly. Lint 0 errors / 17 warnings, typecheck clean, unit 466/466, build clean, main bundle 144.79 kB brotli (limit 200 kB). Playwright validates both `==> ` and `<== ` triggers fire under the default URL (CM6 bridge mode).

### Changed
- **Misc cleanup — bezier-easing v2 shim removed, patch dep updates, eslint ignore list pruned** — `src/services/animationSvc.ts` no longer attaches `.get(t)` / `.toCSS()` methods to bezier-easing's plain function (those were the v2 API; v3 returns a bare `(t) => y`). Easings are now `{ fn, css }` records and the two consumer call sites read `.easing.fn(progress)` and `.easing.css` directly. Patch updates: `@vitest/coverage-v8` 4.1.4 → 4.1.5, `vitest` 4.1.4 → 4.1.5, `eslint-plugin-vue` 10.8.0 → 10.9.0, `abcjs` 6.6.2 → 6.6.3, `dompurify` 3.4.0 → 3.4.1. Skipped major bumps that need framework-level work: `eslint` 9 → 10 (config-breaking), `vite` 7 → 8, `pinia` 2 → 3 / `@vue/test-utils` 1 → 2 (both Vue-3-only), `vue` 2.7 → 3 (Stage 4). `eslint.config.mjs` ignore list pruned — `src/libs/pagedown.js` and `src/libs/clunderscore.js` were deleted in earlier stages and the entries no longer matched anything. Lint 0 errors / 17 warnings, typecheck clean, unit 466/466, build clean, main bundle 144.8 kB brotli (limit 200 kB).
- **In-tree markdown-it plugins ported to TypeScript** — `src/extensions/libs/markdownItTasklist.js` (40 lines), `markdownItAnchor.js` (53 lines), `markdownItMath.js` (59 lines), and `src/libs/markdownItImsize.js` (176 lines) all converted to `.ts`. Adds `@types/markdown-it@^14.1.2` as a devDep so plugin signatures (`MarkdownIt`, `Token`, `StateInline`, `StateCore`) are properly typed instead of implicit `any`. New `src/extensions/libs/types/tokens.ts` declares the `headingContent` / `headingAnchor` token augmentations the anchor plugin attaches for the TOC builder. The two `parseImageSize` helpers in imsize gain proper return-type interfaces; the inline-State construction uses a single typed cast at the call site (markdown-it's typings don't expose the State constructor cleanly). Lint 0 errors / 17 warnings, typecheck clean, unit 466/466, build clean, main bundle 144.87 kB brotli (limit 200 kB; +0.13 kB from the new type imports — negligible). End-to-end Playwright fixture in the preview confirms anchors render `<h1 id="...">`, task lists render the `.task-list-item-checkbox` span, KaTeX picks up `inline_math` tokens, and imsize emits `<img width="..." height="...">`.

### Added
- **Stage 3 batch 11 — cutover: CM6 is now the default editor; pagedown deleted** — `editorSvc.createClEditor` always builds a `cm6ClEditorBridge` (the `?cm6live=1` flag check is gone, the legacy `cledit(editorElt, parent, true)` path is removed). `App.vue.created` unconditionally dynamic-imports the bridge module before `ready=true` so Layout sees the factory wired in synchronously. `editorSvc.pagedownEditor` is gone — link / image dialogs are wired at the call site via `linkCommand(modalOpener) / imageCommand(modalOpener)` factories that pass `cb => useModalStore().open({ type, callback: cb })`. `NavigationBar.pagedownClick`, `NavigationBar.applyHeading`, `optional/shortcuts.ts pagedownHandler`, and `CommandPaletteModal.vue`'s buildCommands all dispatch directly through `cm6Commands` registry. `EditorClassApplier.js` removed its DOM-mutation fallback; the CM6 decoration path is the only path. `optional/keystrokes.ts` deleted — list-continuation Enter and indent-with-Tab are covered by `markdownKeymap` from `@codemirror/lang-markdown` plus CM6's `indentWithTab` (the cledit auto-renumbering of ordered lists on Tab is a feature regression tracked as a follow-up). New `src/services/editor/sharedUtils.ts` hosts `defer / debounce / findContainer` (the only `cledit.Utils` exports reached from outside cledit/); consumers (`editorSvc.ts`, `editorSvcUtils.ts`, `tempFileSvc.ts`, `EditorClassApplier.js`, `PreviewClassApplier.js`, `FindReplace.vue`) now import from `sharedUtils` directly. New `src/services/editor/cm6/cm6MarkerClass.ts` hosts the `Cm6Marker` data class so stores (`content.js`) + components (`FindReplace.vue`) can import it without dragging the `@codemirror/*` tree into the main bundle. **Deleted: `src/libs/pagedown.js` (1372 lines) and `src/services/optional/keystrokes.ts` (193 lines).** `cledit/` source remains for two niche editors (`CodeEditor.vue` settings YAML, `gutters/NewComment.vue` comment textarea) — those are 'soft cutover' targets for a follow-up batch when convenient. Lint 0 errors / 17 warnings, typecheck clean, unit 466/466, build clean, **main bundle 144.74 kB brotli** (limit 200 kB — dropped 4 kB vs batch 10 since pagedown is no longer statically imported). Playwright deep audit at the default URL: CM6 mounts, welcome doc loads, toolbar bold via cm6Commands works, customToolbarButtons inline code works, Cmd-Z undo works, Cmd-F opens find/replace, zero console errors on load.
- **Stage 3 batch 10 — toolbar dispatch through `cm6Commands`** — last batch before the cutover. Adds 11 pagedown-equivalent commands to the registry: `ulist`, `olist`, `clist`, `quote`, `code` (inline-vs-fence based on selection multiline-ness), 6 heading levels (`heading1–6` via a `headingCommand(level)` factory that strips an existing prefix instead of nesting), plus `linkCommand(modalOpener)` / `imageCommand(modalOpener)` factories that call back into the existing link/image modal callbacks (`callback(url | null)` matching the existing `editorSvc.pagedownEditor.hooks.set('insertLinkDialog', cb => useModalStore().open({ type: 'link', callback: cb }))` contract). Plus a `prefixLines(view, prefix)` primitive shared between the four list / quote commands. `NavigationBar.pagedownClick` and `shortcuts.ts pagedownHandler` now check `editorSvc.clEditor.view` — when the bridge is active they dispatch through `cm6Commands`, falling back to `pagedownEditor.uiManager.doClick(name)` for the cledit path. `applyHeading` similarly routes through `heading${N}Command` under bridge mode. Pagedown's chunk-based wrapping is no longer reachable from any consumer when the bridge is active — pagedown stays imported only because `editorSvc.ts` still constructs the `pagedownEditor` instance, which batch 11 removes. New unit tests (`cm6CommandsBatch10.spec.js`, 15 specs) cover list/quote prefixers, code inline-vs-fence behavior, heading-prefix replacement, and link/image factory callback flow incl. null cancellation. Test count 451 → 466. Lint 0 errors / 17 warnings, typecheck clean, build clean, main bundle 149.99 kB brotli (limit 200 kB), Playwright deep audit shows toolbar bold + inlineCode + find/replace all work + zero console errors.
- **Stage 3 batch 9 — scroll sync against CM6 viewport** — `sectionUtils.measureSectionDimensions` previously read `nextSectionDesc.editorElt.offsetTop` (the rendered `<div class="cledit-section">` wrapper that cledit's highlighter creates per section). CM6 doesn't render those wrappers — section descs from the bridge path have no `editorElt`, so cledit-mode measurement collapsed every section to offset 0 and scroll-sync stopped working under `?cm6live=1`. New `resolveEditorSectionTop` helper falls back to `view.coordsAtPos(charOffset)` when `editorElt` is undefined, deriving char-offset from cumulative section-text lengths. Coordinate translated to scroll-container-relative offsetTop via `coords.top - editorElt.getBoundingClientRect().top + scrollTop`. `EditorSvcLike` typed to expose the optional `clEditor.view` so the typecheck passes without `@ts-nocheck`. Pure additive change — cledit path measurement is byte-identical (same `offsetTop` read when `editorElt` is present). Lint 0 errors / 17 warnings, typecheck clean, unit 451/451, build clean, main bundle 148.61 kB brotli (limit 200 kB), Playwright `audit-cm6-live-full.mjs` stays green.
- **Stage 3 batch 8 — EditorClassApplier → CM6 decorations** — replaces the DOM-mutation path used by discussion gutter highlighting + find-match highlighting with a CM6-native `Decoration.mark` set. New `src/services/editor/cm6/cm6Decorations.ts` exposes a `classRangeField` (StateField with auto-mapping across edits via `tr.changes.mapPos(from, 1)` / `tr.changes.mapPos(to, -1)`), plus `addClassRange / removeClassRange / updateClassRange` effect helpers. Bridge wires the field into its base extensions and surfaces matching `bridge.addClassRange / removeClassRange / updateClassRange` methods that return / accept ids. `EditorClassApplier.js` detects bridge mode (presence of `addClassRange` on `clEditor`) and tracks a single CM6 range id per applier; on offset change it dispatches an `updateClassRange` instead of unwrapping + rewrapping a DOM range. `Editor.vue`'s discussion-event walker reads both `elt.discussionId` (cledit JS property) and `elt.dataset.discussionId` (CM6 attribute) so click / hover work in either mode. New unit tests (`cm6Decorations.spec.js`, 7 specs) cover add / remove / update, auto-mapping across an insert, range collapse on full-content delete, span rendering with the configured class, attribute rendering. Test count 444 → 451. Lint 0 errors / 17 warnings, typecheck clean, build clean, main bundle 148.47 kB brotli (limit 200 kB), Playwright `audit-cm6-live-full.mjs` stays green on both flag-off (cledit) and flag-on (CM6 + decoration-driven highlights) paths.
- **Stage 3 batch 7 — bridge polish (pagedown surface)** — final batch of the autonomous Stage 3 session. Adds `bridge.setSelection(start, end)` and `bridge.adjustCursorPosition()` (no-op) so pagedown's `TextareaState.restore` finds the methods it expects on `Object.create(bridge)` after a toolbar wrap. Eliminates the `inputArea.setSelection is not a function` console error that fired after every pagedown button click under `?cm6live=1`. Verified end-to-end: toolbar bold + customToolbarButtons inline code both wrap selection + return cleanly, zero console errors. **Deletion of cledit + pagedown is deferred** — `EditorClassApplier` does DOM manipulation (incompatible with CM6), `optional/scrollSync.ts` measures `.cledit-section` element offsets (CM6 doesn't render those wrappers), and pagedown's UIManager still drives the toolbar dispatch. Each gap requires its own batch (decorations port, viewport-API scroll sync, NavigationBar rewire to `cm6Commands`). The `?cm6live=1` flag is the right place to stage that work; cledit + pagedown stay default until parity ships. Lint 0 errors / 17 warnings, typecheck clean, unit 444/444, build clean, main bundle 148.3 kB brotli (limit 200 kB).
- **Stage 3 batch 6 — live CM6 mount under `?cm6live=1`** — first integration step where the CM6 bridge actually drives the live editor. With `?cm6live=1` in the URL, `editorSvc.createClEditor` builds a `cm6ClEditorBridge` instead of cledit; CM6 mounts inside the existing `.editor__inner <pre>` so `editorSvc.editorElt` keeps pointing at the same DOM node external consumers reach for. `cledit.Marker` is monkey-patched to `Cm6Marker` so the ~15 existing `new cledit.Marker(offset, trailing)` call sites use the bridge-aware marker without per-site changes (full call-site cleanup lands in batch 7). The bridge module is loaded via dynamic `import()` in `App.vue.created` (after `networkSvc.init`, before `ready=true`) — flag-off path stays byte-identical and the heavy CM6 chunk doesn't enter the main bundle. A factory-injection point (`setCm6BridgeFactory(factory, MarkerCtor)` exported from `editorSvcDiscussions`) lets the dynamic import wire itself in without `editorSvcDiscussions` statically importing CM6 (which would otherwise inline ~150 kB of CM6 + Lezer into the main chunk and blow the size-limit gate). Layout.vue stays synchronous so Toc / gutter mounts still see `editorSvc.editorElt` set in time. Lint 0 errors / 17 warnings, typecheck clean, unit 444/444, build clean, main bundle 148.25 kB brotli (limit 200 kB), Playwright `_local/audit-cm6-live-full.mjs` covers four scenarios: (a) flag-off keeps cledit unchanged with `.cledit-section` markers present, (b) flag-on mounts CM6 and loads the welcome doc into the preview, (c) typing in CM6 re-renders the preview heading, (d) Cmd/Ctrl-Z undoes the last edit batch.
- **Stage 3 batch 5 — `cm6ClEditorBridge` (clEditor-shaped CM6 bridge)** — drop-in replacement for the cledit object that the rest of the app reaches for via `editorSvc.clEditor`. Mounts a CM6 `EditorView` under the hood and exposes the full surface enumerated from `grep clEditor\.X src/`: `selectionMgr.{selectionStart, selectionEnd, hasFocus, setSelectionStartEnd, on('selectionChanged'), createRange, getSelectedText, getCoordinates}`, `undoMgr.{canUndo, canRedo, undo, redo, on('undoStateChange'), setCurrentMode, setDefaultMode}`, `watcher.noWatch`, `addMarker`/`removeMarker` (proxied to the `markerField` StateField), `addKeystroke` (no-op pending CM6 keymap wiring), `init`/`destroy`/`focus`/`toggleEditable`, `getContent`/`setContent`/`replace`/`replaceAll`, `on`/`off`, `highlighter.on('highlighted')`. Init transactions carry `Transaction.addToHistory.of(false)` so file switches don't pollute the undo stack. Marker offsets are mirrored from the StateField back to the `Cm6Marker` instances on every doc change so external readers (discussion gutters) see live values without polling. Full-doc replacements use `diff-match-patch` to localize the actual change span — same logic cledit uses, so `setContent(text, suppress)` returns the same `{ start, end, range }` shape and consumers in `editorSvcDiscussions` work unchanged. New `cm6CleditCompat` namespace exposing `Marker` so call sites doing `new cledit.Marker(offset, trailing)` keep working with a one-line import swap. **No consumers swapped yet** — bridge is sandbox-only until batch 6 introduces the `?cm6live=1` dual-mount. New unit tests (`cm6ClEditorBridge.spec.js`, 23 specs) cover content/selection round-trips, replace + replaceAll, event emission (contentChanged + selectionChanged + undoStateChange + highlighted with off-removes-listener), undo/redo (history depth, undo, redo), markers (addMarker/removeMarker, offset auto-update on insert, trailing semantics), toggleEditable, focus, and init lifecycle (synthesized contentChanged with sectionParser output). Test count 421 → 444. Lint 0 errors / 17 warnings, typecheck clean, build clean, main bundle 148 kB brotli (limit 200 kB).
- **Stage 3 batch 4b — long-tail CM6 toolbar commands** — second half of batch 4. Adds 27 more commands using batch 4a's primitives: `mathCommand` (inline `$$` for selection, block `$$\n…\n$$` otherwise), `mermaidCommand` (selected text becomes a labeled flowchart node, default A→B otherwise), `musicCommand` (selected text becomes the ABC piece's `T:` title), 5 callouts (`calloutNote/Tip/Important/Warning/Caution` via a `calloutCommand(type)` factory), 3 ISO date inserts (`date`, `dateTime`, `time`), `frontmatterCommand` (no-op when doc already starts with `---`), 6 case-convert variants (`upper/lower/title/sentence/snake/kebab` via `transformSelection`), `sortLinesCommand` (locale-aware over selected lines via new `transformLines` primitive), `wikiLinkCommand` (`[[Page Name]]` wrap), `imageWithSizeCommand` (markdown-it-imsize syntax with optional alt from selection), 5 table sizes (`table2x2/3x3/4x3/5x4/10x4` via a `tableInsertCommand(rows, cols)` factory + exported `buildTable` helper), and a `specialCharCommand(char)` factory + `SPECIAL_CHARS` palette of 24 typographic / math / arrow chars. Two new primitives (`transformLines`, `transformSelection`) live alongside the existing `wrapSelection` / `insertBlock` / `insertText`. Registry `cm6Commands` now exposes 35 named commands keyed by method. Skipped (need live integration): `footnote` (cross-doc reference counter), `linkFromClipboard` (async clipboard read), `tidy` (whole-doc transform — uses `setContent`, not yet bridged). 33 new unit tests. Test count 388 → 421. Lint 0 errors / 17 warnings, typecheck clean, build clean, main bundle 148 kB brotli (limit 200 kB).
- **Stage 3 batch 4a — CM6 toolbar command primitives + starter set** — fourth step of the cledit + pagedown → CodeMirror 6 migration. Adds `src/services/editor/cm6/cm6Commands.ts` with three primitives that mirror cledit's `customToolbarButtons.js` helpers — `wrapSelection(view, { prefix, suffix, placeholder })`, `insertBlock(view, block)` (preserves the same blank-line padding behavior — `padForBlock` is a verbatim port; cursor lands at end-of-block content), `insertText(view, text)` — plus 8 starter commands wired via these primitives: `bold`, `italic`, `inlineCode`, `highlight`, `subscript`, `superscript`, `strikethrough`, `horizontalRule`. Exposed via a `cm6Commands` registry keyed by method name, ready for the `NavigationBar.vue` swap in batch 6. Long tail (callouts, dates, frontmatter, case-convert, sort-lines, special chars, math, mermaid, footnote, music, image-with-size, table dropdown) deferred to **batch 4b**. New unit tests (`cm6Commands.spec.js`, 16 specs) cover wrap with/without selection / default suffix; insertBlock at empty doc / after content / no double-pad / cursor placement; insertText insert and replace; the 8 starter commands' output strings; and the registry shape. Test count 372 → 388. Lint 0 errors / 17 warnings, typecheck clean, build clean, main bundle 148 kB brotli (limit 200 kB).
- **Stage 3 batch 3 — CM6 marker + selection bridge** — third step of the cledit + pagedown → CodeMirror 6 migration. Adds `src/services/editor/cm6/cm6Marker.ts` (a CM6 `StateField` of `{ id, offset, trailing }` entries that auto-map across every transaction via `tr.changes.mapPos(offset, trailing ? -1 : 1)` — same offset semantics cledit's `Marker.adjustOffset(diffs)` provides today, expressed as CM6's assoc bias) and `src/services/editor/cm6/cm6Selection.ts` (`getSelection(view) / setSelection(view, range) / hasFocus(view) / focusEditor(view)` — mirrors cledit's `selectionMgr` API so future batches 4 / 5 / 6 swap call sites without each rewriting the same boilerplate). The marker field is now in the leaf editor's default extensions; the sandbox builds with markers attached even though no consumer wires them yet — the editorSvcDiscussions / FindReplace bridge work lands in batches 4 + 5. New unit tests (`cm6Marker.spec.js`, 11 specs) lock the marker offset semantics: insert at non-trailing marker pushes it forward; insert at trailing marker leaves it; insert before always shifts; insert after never shifts; delete spanning marker clamps to deletion boundary; multiple markers track independently. Plus 3 selection round-trip tests. Test count 361 → 372. Lint 0 errors / 17 warnings, typecheck clean, build clean, main bundle 148 kB brotli (limit 200 kB), Playwright smoke confirms both flag states stay green.
- **Stage 3 batch 2 — CM6 highlighter + section-list bridge** — second step of the cledit + pagedown → CodeMirror 6 migration. Adds `src/services/editor/cm6/cm6Highlighter.ts` (a StackEdit-flavored `HighlightStyle` mapping `@lezer/highlight` tags to CSS for headings/strong/emphasis/strikethrough/link/url/monospace/list/quote/labelName/processingInstruction; replaces the leaf's generic `defaultHighlightStyle`) and `src/services/editor/cm6/cm6SectionParser.ts` (`parseSectionsForCm6(converter, text)` — a thin pure-function wrapper around `markdownConversionSvc.parseSections` that returns the same `{ data, text }[]` shape the rest of the app reads). Wrapping the bridge in a CM6 `ViewPlugin` is deferred to batch 3 when the editorSvc / CM6 integration lands. New unit tests (`cm6SectionParser.spec.js`, 4 specs) lock the bridge to `markdownConversionSvc.parseSections` section-by-section, plus null-converter handling and full-input round-tripping. Test count 357 → 361. Lint 0 errors / 17 warnings, typecheck clean, build clean, main bundle 148 kB brotli (limit 200 kB), Playwright smoke confirms the sandbox now renders headings and inline `**bold**` with proper visual weight when the flag is on, and the flag-off path stays byte-identical.
- **Stage 3 batch 1 — CodeMirror 6 leaf installed (feature-flagged)** — first step of the cledit + pagedown → CodeMirror 6 migration. Adds 6 CM6 packages (`@codemirror/state`, `@codemirror/view`, `@codemirror/commands`, `@codemirror/language`, `@codemirror/search`, `@codemirror/lang-markdown`) and a small `src/services/editor/cm6/cm6Editor.ts` helper that builds a minimal `EditorView` (line numbers, history, drawSelection, bracket matching, search keymap, markdown language, default highlight style, line wrapping, default + history + indent-with-tab keymaps). Behind a `?cm6=1` query-string flag a sandbox panel mounts in the bottom-right corner of the editor, alongside cledit — proving the leaf works end-to-end without disturbing the live editor. Flag-off path is byte-identical (CM6 chunk is 540 KB raw / 185 KB gzip, code-split via dynamic `import()`; the flag-checker `cm6Flag.ts` lives in its own module so the static import in `Editor.vue` doesn't drag the heavy chunk into the main bundle). Added 4 unit tests under `test/unit/components/cm6Editor.spec.js` (357/357 total). Bundle gate still passes — main bundle 148 kB brotli (limit 200 kB). Lint 0 errors / 17 warnings, typecheck clean, build clean, Playwright smoke covers both `?cm6=1` (sandbox renders + typing reaches CM6 view) and the default flag-off path (cledit unchanged, no sandbox div).
- **Component test coverage expanded — +106 specs** — three new spec files under `test/unit/components/`: (1) `simpleModals.spec.js` (45 specs) — verifies all 20 modal types are present, contentHtml is callable, confirmation modals expose both reject + resolve labels, info modals expose at least one button label, the `textStats` modal renders the multi-section structure correctly with section headers and separators, the `bulkDeletion` modal pluralizes counts based on config. (2) `toolbarButtons.spec.js` (53 specs) — verifies the `pagedownButtons` registry includes the core formatting actions (bold/italic/heading/strikethrough/lists), every entry with a method has title + icon, the `customToolbarButtons` registry exposes all wave 1–4 features (inlineCode, highlight, sub, sup, math, mermaid, footnote, music, tidy, callout, dateTime, frontmatter, convertCase, sortLines, specialChars, wikiLink, linkFromClipboard, imageWithSize, tableInsert, textStats), each has the right shape, dropdowns expose `items[]`, callout has all 5 GFM alert types, convertCase has 6 case transforms, tableInsert has 5 preset sizes, all method names are unique. Mocks the store import so the static-shape inspection doesn't trigger localDbSvc's localStorage-at-module-init side effect. (3) `modalStore.spec.js` (8 specs) — verifies the modal Vuex module's open/reject/stack/hideUntil flows: open() with string vs object normalizes to `{ type, ... }` config, resolve removes the config from the stack, reject rejects the promise and removes the config, stacking works (newest at index 0), hideUntil flips state.hidden during the awaited work and restores it on reject too, getter respects state.hidden. Test count 255 → 361, files 20 → 23. README badge + tests section refreshed.
- **Welcome file expanded with the wave 1–4 surface area** — `src/data/welcomeFile.md` was inherited largely verbatim from upstream (intro, Files, Sync, Publication, SmartyPants, KaTeX, UML, Music). It now also demos the default-enabled markdown extensions that previously went unmentioned (tables, GFM task lists, footnotes, strikethrough/highlight/sub/sup, definition lists, abbreviations, image dimensions, emoji), introduces the **command palette** (`Ctrl/⌘ + K`) up front so new users discover the discoverability hub, and adds a closing **Editor power features** section covering view toggles (focus mode / scroll sync / line numbers / reader mode), find & replace, drag-and-drop import (files + folders + images), the TOC outline fold, the heading picker, callouts, YAML front-matter, wiki links, case-convert, sort-lines, special-characters, insert-date, link-from-clipboard, auto-link-on-paste, the tidy magic-wand, and the `==>` / `<==` text-expansion arrows. Image-dimensions demo uses a small original `static/welcome-image.svg` (1.5 KB; sharp-edged editor-+-preview mockup in StackEdit's brand palette) — same-origin, served at `/welcome-image.svg` via Vite `publicDir`, no external image dependency. The demo uses the bundled markdown-it-imsize syntax `![alt](url =WIDTHxHEIGHT)` (note the **space before `=`**) — verified end-to-end: the in-browser preview renders the `<img>` at exactly the requested 360×180 pixels with `width` and `height` attrs set. The Hugo/kramdown/pandoc `{width=N}` attribute syntax is **not** supported by the bundled extension and renders as literal text after the image — the welcome file calls this out explicitly. Brand-new workspaces bootstrap with the new content (`localDbSvc.js` only creates a welcome file when the workspace is otherwise empty); existing users keep whatever they have — the old welcome file isn't auto-replaced. Its hash is added to `welcomeFileHashes` on boot so unedited welcome files (old or new content) keep being treated as skip-sync "temp" files in `syncSvc.isTempFile`.
- **Wave 4 — 8 deferred-roadmap features shipped**:
  - **Auto-link on paste** — pasting a bare URL while text is selected wraps it as `[selected](url)` instead of replacing. URL trimmed before testing; non-URL pastes go through the unchanged paste-as-text path.
  - **Command palette** (Cmd / Ctrl + K) — modal popover with fuzzy-search across all 37+ toolbar actions plus expanded dropdown items. Arrow-key nav, Enter executes, Escape closes. Cmd / Ctrl + / opens the same palette (insert-style alias). Backed by a new `CommandPaletteModal.vue` and a global tinykeys binding registered alongside the user-configurable shortcuts.
  - **PWA install / update UX** — service-worker `onNeedRefresh` now confirms via `notification/confirm` ("A new version of StackEdit is ready. Reload now?") instead of silently force-reloading. Install prompt was already wired; update prompt now matches that pattern. Local-db sync still flushes before reload regardless of choice.
  - **Slash commands** (Cmd / Ctrl + /) — opens the same command-palette popover. Full at-cursor `/` detection requires editor-level work that's deferred (see PRD); the keyboard alias is the MVP.
  - **Sync auto-merge surfaces** — `syncSvc.js` now dispatches a `notification/info` when a real 3-way conflict happened (server *and* client edited the same content from the last-merged baseline). Previous behavior was silent — users had no indication their edits were recombined non-trivially.
  - **Better print stylesheet** — explicit `@media print` rules hide nav-bar / button-bar / explorer / side-bar / status-bar / editor / gutter, normalize the preview's font to 11pt + black, add page-break-avoid rules for headings / pre / blockquote / table / mermaid / images, set `orphans: 3; widows: 3` on paragraphs, and append link URLs after the link text via `a[href^="http"]::after { content: " (" attr(href) ")" }`. New `@page { margin: 1.5cm }`.
  - **Drag-and-drop image upload** — dropping image files (any number) onto the editor inserts each as a base64 data URL (`![filename](data:image/...)`). Mirrors the existing paste-image path. Sequential drops insert with newlines between them. Bypasses any configured upload provider — works offline.
  - **Outline fold for the TOC sidebar** — new H1/H2/.../H6 button row at the top of the in-app TOC panel. Click `H2` to hide everything below H2; click `H6` to show all. Pure CSS via `.toc__inner--fold-N` cascading `display: none` rules. State is per-mount (resets when sidebar closes/opens). Full collapse-by-heading inside the editor itself is gated on a CodeMirror 6 migration — see PRD.
- **Wave 3 toolbar additions (4 more)** — image-with-dimensions (`![alt](url){width=300}` template — Hugo / kramdown / pandoc syntax), insert-table dropdown (2×2 / 3×3 / 4×3 / 5×4 / 10×4 preset sizes generating header + separator + body), text-statistics (chars/words/lines/sentences/reading-time alert for current selection or whole doc), and a **line-numbers toggle** in the button-bar (new `showLineNumbers` layoutSetting + `toggleLineNumbers` action; renders an absolutely-positioned gutter with a span-per-source-line counter that updates on every `sectionList` event). Reuses `file-image`, `table`, `information`, and `format-list-numbers` icons. Validation: 41/41 Playwright checks pass including new line-number gutter on/off.
- **Slimmer toolbar buttons** — `.navigation-bar__button` width reduced 34 → 30 px, padding 0 7px → 0 5px (saves ~144 px of toolbar width across the ~37-button row). Necessary to keep the wave-2/3 toolbar from wrapping to a clipped second row on common 1366–1920 px viewports. SVGs render fine at the slimmer footprint.
- **Eight more daily-use toolbar buttons** — second wave on top of the v5.16.0 toolbar set. Implementations:
  - **Callout / admonition** (dropdown: Note / Tip / Important / Warning / Caution) — inserts GitHub-flavored `> [!TYPE]` blockquote alerts. Selection becomes the body; otherwise a placeholder line is dropped in.
  - **Insert date** (dropdown: Date `YYYY-MM-DD` / Date+time / Time only) — ISO format, locale-neutral, sortable, what static-site generators expect.
  - **YAML front-matter** — inserts a `title / author / date / tags` stub at document start, cursor lands at end of `title: ` ready to type. No-op if the doc already starts with `---` (don't double-stamp).
  - **Convert case** (dropdown: UPPERCASE / lowercase / Title Case / Sentence case / snake_case / kebab-case) — operates on selection only.
  - **Sort lines alphabetically** — `localeCompare`-based sort of lines covered by selection (or current line block).
  - **Special characters palette** (24-entry dropdown) — em-dash, en-dash, ellipsis, curly quotes, arrows, ©®™, fractions, math operators, etc.
  - **Wiki-style link** (`[[Page Name]]`) — wraps selection in double-bracket syntax for export to Obsidian / Foam / Logseq.
  - **Link from clipboard** — reads `navigator.clipboard.readText()`; if it's a URL, wraps selection (or `link text` placeholder) as `[text](url)` in one click. Falls through to `[text](https://)` if clipboard is empty / not a URL / permission denied.
  - Seven new MDI icons (Calendar, CaseSensitiveAlt, SortAlphabetical, Omega, LinkBracket, LinkPaste, Frontmatter) plus reuse of existing Alert. Dropdown buttons (`dropdown: true`, `items: [{ name, perform }]`) flow through a shared `openCustomDropdown` in `NavigationBar.vue` that anchors a `contextMenu/open` popover under the button. End-to-end Playwright validation (`_local/validate-features.mjs`) confirms 33/33 button-and-dropdown checks pass. Lint + typecheck + 247 unit tests stay green.
- **Music notation example in the Welcome file** — added a "Music notation" section under the UML diagrams section showing an ABC-notation block with the opening of "Twinkle, Twinkle Little Star". Mirrors the existing Mermaid + KaTeX examples so the new music-notation toolbar button has something to demonstrate when users click it on a fresh workspace.
- **Tidy-markdown toolbar button** — magic-wand icon at the end of the toolbar runs a conservative whole-document tidier. Outside fenced code blocks: trims trailing whitespace per line, collapses 3+ blank lines to 1 blank, ensures a space after heading hashes (`#foo` → `# foo`) and list-bullet markers (`-foo` → `- foo`), normalizes EOF to exactly one trailing newline. Inside fenced code blocks: leaves everything alone (whitespace there is intentional). Deliberately doesn't reflow paragraphs, rewrap tables, or change emphasis-marker style — those are opinionated transforms that can damage intentional formatting. cledit's `setContent` uses diff-match-patch internally, so cursor position is preserved for unchanged portions.
- **Nine new toolbar buttons for already-supported features** — features that StackEdit already shipped via markdown-it plugins finally got UI affordance. Added: **inline code** (`` `...` `` wrap), **highlight** (`==…==`), **subscript** (`~…~`), **superscript** (`^…^`), **horizontal rule** (`---` block insert), **math** (KaTeX `$inline$` for selection or `$$\nE = mc^2\n$$` block when no selection), **mermaid** (` ```mermaid\nflowchart LR\n  A --> B\n``` ` block stub), **footnote** (auto-numbered `[^N]` reference at cursor + matching `[^N]: …` definition appended), **music** (ABC notation block via the bundled abcjs lib). Implemented via a new `src/data/customToolbarButtons.js` with `action(editorSvc)` functions that operate on `cledit` directly (bypasses pagedown's UIManager since it doesn't know these commands). Nine new MDI icons (Math, Sitemap, HorizontalRule, Marker, Footnote, FormatSubscript, FormatSuperscript, CodeInline, MusicNote) registered globally. Old pagedown buttons still flow through pagedown's existing handler.
- **Heading button is now a level picker (H1-H6)** — replaces pagedown's cycle-through-three-levels behavior. Click opens a context-menu popover anchored under the button with H1 through H6 entries; pick one and the current line gets the matching `# ` prefix (existing heading prefix stripped, so re-applying replaces rather than nests). Exposes H4-H6 which the cycle button never reached.
- **Component test coverage scaffolding** — added `test/unit/components/` and the first 16 specs (231 → 247 total). Covers `defaultLocalSettings` (filePropertiesTab default, publish-template defaults, no-mutation guarantee) and the icon registry (all bundled icons resolve via `Vue.component(name)`, including the recently-added chevrons + language icons). Vitest resolver gained `.vue` to its `extensions` list to match the production Vite config so `import Provider from './Provider'`-style icon-registry imports resolve in tests too. Establishes the pattern for further component specs (Explorer, sync flow, modals) without committing to a full @vue/test-utils mount harness up front.
- **Bundle-size gate in CI** — `size-limit` (`@size-limit/preset-app`) checks 7 per-chunk budgets on every PR after build: main bundle ≤ 200 KB, mermaid (lazy) ≤ 800 KB, prism ≤ 230 KB, abcjs ≤ 175 KB, katex ≤ 90 KB, markdown-it ≤ 80 KB, vue ≤ 40 KB (all compressed; size-limit uses brotli when smaller). Catches regressions where an innocent dep bump or import-of-default-export inflates a chunk. New `npm run size` script + new "Bundle size" CI step. Current bundle profile lands well under all budgets — budgets sized at ~10–25% headroom over the v5.16.0 baseline.
- **TypeScript on the `api/` layer** — all 9 Vercel API routes (`_utils`, `conf`, `cspReport`, `googleDriveAction`, `health`, `userInfo`, `githubToken`, `pdfExport`, `pandocExport`) ported from `.js` to `.ts`. Edge-runtime handlers typed with the standard Web `Request` / `Response` types; Node-runtime handlers (`githubToken`, `pdfExport`, `pandocExport`) typed with `@vercel/node`'s `VercelRequest` / `VercelResponse`. New `tsconfig.json` (root) scoped to `api/**/*.ts` with strict mode + `moduleResolution: bundler`. New `npm run typecheck` script + CI step (`tsc --noEmit`). The 4 existing `_utils` hardening specs still pass unchanged. Vite handles `.ts` in tests natively (esbuild). Vercel auto-detects `.ts` in `api/` and compiles at deploy time. SPA stays JS — this is scoped to the security-critical surface only.

### Changed
- **Pinia batch 9 — root state + getters migrated; Vuex uninstalled** — last batch of the migration. New `src/stores/global.js` Pinia store hosts the four root state fields (`light`, `offline`, `lastOfflineCheck`, `timeCounter`) and the seven cross-cutting root getters (`allItemsById`, `pathsByItemId`, `itemsByPath`, `gitPathsByItemId`, `itemIdsByGitPath`, `itemsByGitPath`, `isSponsor`). The `setOffline` action — which Vuex modeled as an action wrapping a mutation — now lives as `setOffline` action calling `setOfflineRaw` raw write (same name-collision pattern as content/workspace from earlier batches). 17 consumer files swapped via Node script: `store.state.{light,offline,...}` → `useGlobalStore().X`, `store.getters.{allItemsById,...}` → `useGlobalStore().X`, `store.commit('setLight', ...)` → `useGlobalStore().setLight(...)`, `store.dispatch('setOffline', ...)` → `useGlobalStore().setOffline(...)`. `store.watch(getter, callback)` calls (4 files: `localDbSvc`, `tempFileSvc`, `editorSvc`, `optional/scrollSync`, `optional/shortcuts`, `editor/editorSvcDiscussions`) swapped to Vue's `watch` from `'vue'`. `store.subscribe(mutation => ...)` in `uiPersistence.ts` swapped to `useExplorerStore().$subscribe()`. The 30-second `updateTimeCounter` interval moved from `store/index.js` to `src/index.js`. **`src/store/index.js` deleted.** `src/store/moduleTemplate.js` deleted (only documented as historical reference in store-factory comments). 19 leftover `import { mapState, ... } from 'vuex'` lines removed from components. **`npm uninstall vuex --legacy-peer-deps`** — Vuex 3 is gone. The migration is complete: every store is Pinia. Lint 0 errors / 17 warnings, typecheck clean, 353/353 unit tests pass, build clean, Playwright clean.

- **Pinia batch 8 — `discussion` + `explorer` migrated** — last two Vuex modules ported. `discussion` (184 lines) → `src/stores/discussion.js` (172 lines, options-store, with the `newDiscussion` getter renamed to `newDiscussionFromCurrent` to avoid collision with the state field of the same name). `explorer` (390 lines) → `src/stores/explorer.js` (321 lines, options-store, including the heavy `nodeStructure` getter that builds the trash/temp/recent virtual folders, sorts honoring user mode + pinned folders, and computes recursive descendant-file counts). 15 consumer files updated: 5 gutter components, Explorer/ExplorerNode/Layout/Editor/Preview/NavigationBar, plus 4 services (localDbSvc, explorerSvc, uiPersistence, editorSvcDiscussions). Cross-store `vuexStore.getters['discussion/currentDiscussion']` calls in `stores/layout.js` and `stores/data.js` swapped to `useDiscussionStore().currentDiscussion`. `stores/file.js`'s `vuexStore.state.explorer.openNodes` swapped to `useExplorerStore().openNodes`. Vuex root reduced 2 → 0 modules — `store/index.js` now has no `modules:` key. The `pathsByItemId` root getter now reads `useExplorerStore().rootNode` directly. `discussion.js` lazy-loads `syncSvc` via `await import()` inside `createNewDiscussion` so happy-dom unit tests of unrelated services don't drag in `localDbSvc`'s `localStorage` access at module init. Lint 0 errors / 23 warnings (down from 39), typecheck clean, 353/353 unit tests pass, build clean, Playwright clean. **Vuex still loaded for `light`/`offline`/`lastOfflineCheck`/`timeCounter` root state and the `allItemsById`/`pathsByItemId`/`gitPathsByItemId`/`itemIdsByGitPath`/`itemsByGitPath`/`isSponsor` root getters — those move in batch 9, which can `npm uninstall vuex`.**

- **Pinia batch 7 — `data` + `layout` migrated** — heaviest cross-cut yet: `data` is the core key-value store (workspaces, settings, syncData, tokens, templates, ~30 getters covering everything from `computedSettings` to `lastOpenedIds`) at 316 lines / 211 callers; `layout` (197 lines) computes the bodyWidth/bodyHeight + the master `styles` getter that drives every panel's geometry. Both ported to Pinia options-stores at `src/stores/data.js` (308 lines) and `src/stores/layout.js` (177 lines). **~70 consumer files updated** across components, services, providers, helpers, modals, menus — sed swap of `store.dispatch/commit/getters/state` calls plus a Node script that injected `useDataStore` / `useLayoutStore` imports at 4 distinct relative-path depths. **15 components had `mapState/mapGetters/mapActions('data', ...)` or `('layout', ...)`** swapped to Pinia's `mapPiniaState` / `mapPiniaActions` (`Editor.vue`, `Preview.vue`, `Layout.vue`, `ButtonBar.vue`, `Toc.vue`, `StatusBar.vue`, `NavigationBar.vue`, `SideBar.vue`, `Explorer.vue`, gutters, modals, menus). Cross-store cycle (data ↔ workspace) handled via top-level ES-module imports; both `useDataStore` and `useWorkspaceStore` resolve lazily on first call so cycles work without `require()` (Vite ES-modules can't `require` at runtime — caught by Playwright). Vuex root reduced from 4 modules to 2: only `discussion` and `explorer` remain. Vuex root state still has `light`/`offline`/`lastOfflineCheck`/`timeCounter` and the merged-Pinia `allItemsById` / `pathsByItemId` getters — those move in batch 9. `store/discussion.js` updated to call `useWorkspaceStore().loginToken` directly (was `rootGetters['workspace/loginToken']`); `store/explorer.js` reads `useDataStore().lastCreated`. Cleaned up 35 leftover unused `import store from '../store'` / `import { mapMutations, mapGetters } from 'vuex'` warnings via a Node sweep. Lint clean (0 errors, 17 warnings — down from 65), typecheck clean, 353/353 unit tests pass, build clean, Playwright smoke verifies welcome doc renders + edit-to-preview reflects + zero page/console errors.

- **Pinia batch 6 — `workspace` + `publishLocation` + `syncLocation` migrated** — sixth batch handles the cross-cutting provider modules. `workspace` (132-line Vuex module — workspace state + 13 getters that read tokens by sub from data, plus removeWorkspace/patchWorkspacesById that commit back to data) ported to Pinia. `locationTemplate.js` factory ported to `src/stores/locationStoreFactory.js` — extends `itemStoreFactory` with the 5 location-aware getters (groupedByFileId, groupedByFileIdAndHash, filteredGroupedByFileId, current, currentWithWorkspaceSyncLocation). `publishLocation` and `syncLocation` Pinia stores both use the factory. **128 call sites + 12 component-level mapGetters swapped.** Cross-store reads stay live: workspace's getters delegate to data/workspaces, data/googleTokensBySub etc. (still Vuex); location stores' currentWithWorkspaceSyncLocation reads workspace/currentWorkspace + data/syncDataByItemId. Updated Vuex store/explorer.js's `nodeStructure` getter (which reads `syncLocation/filteredGroupedByFileId` + `publishLocation/filteredGroupedByFileId`) and store/layout.js's `computeStyles` (reads syncLocation/current + publishLocation/current) to call the Pinia stores directly. Also updated the root Vuex `isSponsor` getter to use `useWorkspaceStore().sponsorToken` instead of `getters['workspace/sponsorToken']`. Mutation/action collision on `setCurrentWorkspaceId` resolved the same way as content's `setRevisionContent`: the mutation became `setCurrentWorkspaceIdRaw`, the wrapping action keeps the original name. 5 Vuex modules remain (down from 8): data, discussion, explorer, layout — plus the root-level state/getters (light, offline, allItemsById, pathsByItemId, etc.). Lint clean, typecheck clean, 353/353 tests pass, build clean, 4/4 Playwright.

- **Pinia batch 5c — `content` module migrated** — `content` (114-line Vuex module) ported to a 138-line Pinia options-store. Inlined rather than extending `itemStoreFactory` because content adds: `state.revisionContent`, `setRevisionContent` mutation/action pair (which share a name but do different things — the action diffs + commits, the mutation just stores), plus 4 custom getters that cross-cut into `file` (Pinia) and `layout` (still Vuex). 48 call sites + 5 component-level helpers swapped: `Layout.vue`, `NavigationBar.vue`, `HistoryMenu.vue` had `mapState('content', ['revisionContent'])` / `mapMutations('content', ['setRevisionContent'])` / `mapActions('content', ['restoreRevision'])` — swapped to Pinia equivalents. Vuex's mutation/action name collision resolved by renaming the mutation to `setRevisionContentRaw` in Pinia and aliasing it back to `setRevisionContent` for components via `mapPiniaActions(useContentStore, { setRevisionContent: 'setRevisionContentRaw' })` so template/method calls don't change. `store/discussion.js` and `store/layout.js` (still Vuex) updated to call the Pinia content store. Updated the Vuex root `allItemsById` getter and `itemBridge.js` to include `file` and `content` in the Pinia-store registry — without this, `localDbSvc.loadItem()` couldn't find file/content items in the merged map and threw `Error: Data not available.` (caught by Playwright's console-error capture). 8 Vuex modules remain (down from 9): data, discussion, explorer, layout, publishLocation, syncLocation, workspace, locationTemplate. Lint clean, typecheck clean, 353/353 unit tests pass, build clean, 4/4 Playwright.

- **Pinia batch 5b — `file` module migrated (the central state module)** — `file` was the heaviest cross-cut yet: 88 call sites across ~40 consumer files, plus 4 modal components doing `mapGetters('file', ['isCurrentTemp'])`. State (`itemsById`, `currentId`), actions (`setItem`/`patchItem`/`deleteItem`/`setCurrentId`/`patchCurrent`), and three custom getters (`current`, `isCurrentTemp`, `lastOpened`). The `lastOpened` getter reads cross-store: `vuexStore.state.explorer.openNodes` (still Vuex) + `useFolderStore().itemsById` (Pinia) + `vuexStore.getters['data/lastOpenedIds']` (still Vuex). New `src/pinia.js` module is imported FIRST in `src/index.js` and calls `Vue.use(PiniaVuePlugin)` + `setActivePinia(createPinia())` so the active Pinia instance is set up before any service module or Vuex getter that touches a Pinia store. Without this, `scrollSync.ts` registers a Vuex watcher at module load that eagerly evaluates `layout/styles`, which now reads `useFileStore().isCurrentTemp`, and crashes because Pinia isn't mounted yet. `services/uiPersistence.ts`'s Vuex `store.subscribe(mutation => mutation.type === 'file/setCurrentId')` swapped to Pinia's `useFileStore().$subscribe(...)` — same effect, monitors a different store. Updated `contentState`/`syncedContent` Pinia stores to read `current` from the Pinia file store instead of the (now-gone) Vuex one. Updated `store/layout.js`, `store/data.js`, `store/locationTemplate.js`, `store/explorer.js`, `store/content.js` (still Vuex) to import `useFileStore` and read it directly. 9 Vuex modules remain (down from 10): content, data, discussion, explorer, layout, publishLocation, syncLocation, workspace, locationTemplate. Lint clean, typecheck clean, 353/353 unit tests pass, build clean, 4/4 Playwright. Path-fixing caught 1 build error in `services/providers/common/Provider.ts` (path depth wrong); fixed before commit.

- **Pinia batch 5a — `folder` + `syncedContent` + `contentState` + `userInfo` migrated** — fifth batch of the Vuex 3 → Pinia migration; first batch to handle the `moduleTemplate`-based item-stores. Built a Pinia equivalent factory `src/stores/itemStoreFactory.js` that takes the same `(empty, simpleHash)` signature plus optional `extraGetters` / `extraActions` for module-specific extras. Migrated 4 modules: `folder` (uses factory as-is), `syncedContent` (factory + 1 `current` getter that reads `file/current` from Vuex during the transition), `contentState` (factory + `current` getter + `patchCurrent` action), `userInfo` (custom `setItem` that preserves existing `name`/`imageUrl` fields — doesn't fit the factory shape). 28 call sites across 12 files swapped: `store.commit('X/setItem|patchItem|deleteItem', ...)` → `useXStore().setItem|patchItem|deleteItem(...)`; `store.getters['X/items|current']` → `useXStore().items|current`; `store.state.X.itemsById` → `useXStore().itemsById`. Built a transitional bridge `src/stores/itemBridge.js` for runtime-keyed dispatches in `localDbSvc` / `syncSvc` / `workspaceSvc` / `publishSvc` (e.g., `store.commit(`${type}/setItem`, item)` where `type` is determined at runtime — these need to route to either Pinia or Vuex depending on the type). Updated the Vuex root `allItemsById` getter to merge Pinia stores' `itemsById` alongside the still-Vuex modules. Caught one runtime bug at the boundary: typecheck couldn't see the `current` getter on the contentState store because `defineStore`'s generic types don't pick up dynamically-passed `extraGetters` — added an `as any` cast at the one TS callsite (`editorSvcUtils.ts`) until contentState gets a properly-typed wrapper. 10 Vuex modules remain (down from 14 → `content`, `data`, `discussion`, `explorer`, `file`, `layout`, `publishLocation`, `syncLocation`, `workspace`, plus `locationTemplate`). Lint clean, typecheck clean, 353/353 unit tests pass, build clean. Playwright smoke verifies welcome doc + edit-to-preview + zero page/console errors.

- **Pinia batch 4 — `modal` module migrated** — fourth batch of the Vuex 3 → Pinia migration. `modal` (40-line Vuex module — handles the modal stack, hideUntil pause, and config getter) ported to a 30-line Pinia options-store at `src/stores/modal.js` with the same `open` / `hideUntil` action surface and `config` getter. **Largest cross-cut yet — 79 call sites across 33 files**: every modal-opener (file properties, settings, sponsor, sync/publish/workspace management, image picker, account management, GoogleDrive*Modal, etc.) and 14 modal components reading the `config` getter via `mapGetters('modal', ...)`. All `store.dispatch('modal/open', ...)` swapped to `useModalStore().open(...)`. All `store.dispatch('modal/hideUntil', ...)` (3 multiline calls in GoogleDrive*Modal) swapped to `useModalStore().hideUntil(...)`. All Vuex `mapGetters('modal', ['config'])` swapped to Pinia's `mapState as mapPiniaState` against `useModalStore`. Two Vuex root modules (`store/discussion.js`, `store/content.js`) had their cross-module `dispatch('modal/open', ..., { root: true })` calls swapped to direct `useModalStore().open(...)`. Path-fixing caught: relative paths to `src/stores/modal` differ by file location (3 different depths in `src/services/providers/helpers/`, `src/components/modals/common/`, etc.) — initial sed-injected imports needed correction in 4 files before the build succeeded. Updated `test/unit/components/modalStore.spec.js` (8 specs) to use Pinia's `createPinia` + `setActivePinia` test harness. 14 Vuex modules remain. Lint clean, typecheck clean, 353/353 unit tests pass, build clean. Playwright smoke (`_local/audit-pinia-batch3.mjs`) verifies welcome doc renders + edit-to-preview reflects + zero page/console errors (confirmed `[vuex] module namespace not found in mapGetters(): modal/` warning is gone).

- **Pinia batch 3 — `notification` module migrated** — third batch of the Vuex 3 → Pinia migration. `notification` (90-line Vuex module) ported to a 75-line Pinia options-store at `src/stores/notification.js` with the same `info` / `badge` / `confirm` / `error` action surface. Largest consumer cross-cut yet: 35 call sites across 24 files (services, providers, components, modals, menus). All `store.dispatch('notification/<action>', ...)` swapped to `useNotificationStore().<action>(...)`; Vuex `mapActions('notification', ...)` in 5 components (CurrentDiscussion, SyncManagementModal, PublishManagementModal, WorkspaceManagementModal, HtmlExportModal) and `mapState('notification', ...)` in 1 component (Notification.vue) swapped to Pinia equivalents (the components keep both `vuex` and `pinia` imports during the transition since they touch other still-Vuex modules). The Vuex root `setOffline` action's bare `dispatch('notification/info', ...)` swapped to `useNotificationStore().info(...)`, dropping the `dispatch` from its destructured action context. The `queue` Pinia store's `error`-handler also updated to call the Pinia notification store directly instead of `store.dispatch('notification/error', ...)`. 15 Vuex modules remain. Lint clean, typecheck clean, 353/353 unit tests pass, build clean. Playwright smoke (`_local/audit-pinia-batch3.mjs`) verifies welcome doc renders + edit-to-preview reflects + zero page/console errors after migration (confirmed the `[vuex] module namespace not found` warning is gone).

- **Pinia batch 2 — `contextMenu` + `queue` modules migrated** — second batch of the Vuex 3 → Pinia migration. `contextMenu` (55 lines) and `queue` (83 lines) ported to Pinia options-stores at `src/stores/contextMenu.js` (41 lines) and `src/stores/queue.js` (65 lines). 10 consumer files updated: `ContextMenu.vue`, `ExplorerNode.vue`, `NavigationBar.vue`, `syncSvc.ts`, `publishSvc.ts`, `providers/googleDriveProvider.ts`, `menus/SyncMenu.vue` (4 calls), `menus/HistoryMenu.vue` (3 calls), `modals/PdfExportModal.vue`, `modals/PandocExportModal.vue`. Cross-module reads handled gracefully during the transition: contextMenu's offscreen-position math reads `bodyHeight` / `bodyWidth` from the still-Vuex `layout` module via `store.state.layout`; queue's `enqueue` action reads `store.state.offline` (root) and dispatches `store.dispatch('notification/error', ...)` to the still-Vuex `notification` module; the Vuex `notification` module's `error` action reads `useQueueStore().currentLocation` so cross-direction lookups stay live. Also fixed two latent bugs the Playwright smoke caught: `NavigationBar.vue:162` `showSpinner` and `syncSvc.ts:956` `unloadContents` interval read `store.state.queue.isEmpty` — now `useQueueStore().isEmpty`. 16 Vuex modules remain. Lint clean, typecheck clean, 353/353 unit tests pass, build clean. Playwright smoke (`_local/audit-pinia-batch2.mjs`) verifies the welcome doc renders + command palette opens + zero page/console errors after migration.

- **Pinia introduced alongside Vuex; first leaf module (`findReplace`) migrated** — batch 1 of the Vuex 3 → Pinia migration. Added `pinia@^2` (Vue 2.7 compatible via `PiniaVuePlugin`), wired both stores into the root Vue instance in `src/index.js`, established `src/stores/` for Pinia store definitions. `findReplace.js` (Vuex module — 32 lines) ported to a Pinia options-store at `src/stores/findReplace.js` (29 lines, near-identical surface: same `state` factory, mutations folded into `actions`, `open(...)` action delegates to the per-field setters). Updated 3 consumers (`FindReplace.vue`, `Layout.vue`, `services/optional/shortcuts.ts`) to call `useFindReplaceStore()` instead of `store.commit('findReplace/...')` / `store.dispatch('findReplace/...')` / `store.state.findReplace.x`. Vue 2 `mapState` from `vuex` swapped for Pinia's `mapState` from `pinia` — the v-model two-way bindings keep their `accessor()` factory but now read/write through the Pinia store. `findReplace` removed from the Vuex root in `src/store/index.js`. 18 Vuex modules remain. Lint clean, typecheck clean, 353/353 tests pass, build clean. Playwright smoke (`_local/audit-pinia-batch1.mjs`) verifies Cmd+F opens the dialog, the input is reactive, Escape closes, no page/console errors.

- **`clunderscore` removed — replaced 47 call sites across 21 files with native iteration** — `src/libs/clunderscore.js` (the in-tree prototype-extension shim) deleted along with its test spec and shims.d.ts type declarations. Native equivalents: `cl_each` → `forEach` on Arrays / NodeLists; `Array.from(...).forEach(...)` for `HTMLCollection` (which still doesn't have `forEach` in any browser). `cl_map` → `map` for Arrays; `Array.from(coll, mapFn)` for `NodeList`. `cl_some` / `cl_filter` / `cl_reduce` → native `some` / `filter` / `reduce` on Arrays. `cl_bind` → native `Function.prototype.bind`. `cl_extend(other)` → `Object.assign(target, other)`. Caught one latent bug along the way: `utils.uid()` did `array.cl_map(value => alphabet[value % radix]).join('')` where `array` is a `Uint32Array`. The naive `.cl_map → .map` swap would silently break `uid()` (typed-array map coerces the string callback result back to 0; every UID would render as `"0000000000000000"`). Switched to `Array.from(array, mapFn)` which produces a real `string[]`. A separate runtime audit caught two more swaps that needed `Array.from(...)` wrappers — `editor.$markers` is an object literal (`Object.values(...).forEach(...)` not `.forEach(...)`), and four `getElementsByClassName(...)` callsites (`HTMLCollection`, no native `forEach`) in `cleditHighlighter`, `Editor.vue`, `Preview.vue`. Caught both via a Playwright smoke test that types into the editor and verifies the preview re-renders. End state: lint clean, typecheck clean, 353/353 unit tests pass (was 361 — minus the 8 deleted clunderscore specs), build clean, 8/8 Playwright audit checks pass. Net: ~170 lines of vintage prototype-monkey-patching gone, no behavior change, one quiet runtime bug eliminated.

- **TypeScript migration batch 8 — cledit core completes the cledit unit on TS** — final 5 files in `src/services/editor/cledit/` ported: `cleditCore.js → .ts` (522 lines, `@ts-nocheck` — 500+ lines of deep DOM/selection/MutationObserver coordination, full typing requires editor-state interface design that's out of scope for this incremental migration), `cleditSelectionMgr.js → .ts` (431 lines, `@ts-nocheck` for the same reason), `cleditMarker.js → .ts` (49 lines, properly typed — `Marker` class with `id`/`offset`/`trailing` fields, `adjustOffset` takes typed diff-match-patch tuples), `cleditWatcher.js → .ts` (34 lines, properly typed — `MutationObserver` wrapper with `CleditEditor` interface for the editor surface), `index.js → .ts` (11 lines, just imports). The cledit subdirectory is now 100% `.ts` files. Two of seven still carry `@ts-nocheck` (cleditCore + cleditSelectionMgr) — the rest got real types. 31 services on TS now (3+5+4+4+5+4+1+5); ~33 still on JS — about 48% migrated.
- **TypeScript migration batch 7 — `localDbSvc` ported (IndexedDB persistence)** — `localDbSvc.js → .ts` (483 lines). Real types throughout, no `@ts-nocheck`: `DbItem` interface for the IndexedDB row shape, `StoreItemMap` alias, `TxCb` for the deferred-tx callback queue, `LocalDbSvc` interface for the singleton's full surface (so `loadSyncedContent`/`loadContentState` loaders attached after the literal stay properly typed). `Connection` class typed with `IDBDatabase` / `IDBTransaction` / `IDBOpenDBRequest` / `IDBVersionChangeEvent` / `IDBRequest<IDBCursorWithValue>` from the standard DOM lib — caught one minor cleanup (the `getWorkspaceItems` cancel function returned `undefined` from `connection.db?.close()` instead of returning the function itself; preserved). 26 services on TS now (3+5+4+4+5+4+1); ~38 still on JS. Lint clean, typecheck clean, 361/361 unit tests pass, vite build clean. Comprehensive 20/20 Playwright audit re-ran post-port — zero errors.
- **TypeScript migration batch 6 — 4 cledit / discussion files ported with `@ts-nocheck` opt-out** — `editor/cledit/cleditUndoMgr.js → .ts` (176 lines, undo/redo state machine with debounced save buffer), `editor/cledit/cleditKeystroke.js → .ts` (188 lines, default keystroke handlers — undo/redo, tab indent, smart enter, word-jump backspace/arrows), `editor/cledit/cleditHighlighter.js → .ts` (199 lines, syntax highlighting with `<style>` injection per-section), `editor/editorSvcDiscussions.js → .ts` (256 lines, discussion-marker pipeline + class appliers wiring). All 4 files carry `// @ts-nocheck` with a comment explaining why: they're tightly coupled to `cleditCore.js` (still on JS) and the discussion store's dynamic surfaces, so full typing requires porting cleditCore first. Renaming to `.ts` still buys real benefits even with nocheck on: vite/esbuild treats them as TypeScript modules, future incremental refactors (var → const, etc.) flow through, and the migration ledger lists them as "in scope." Plus the legitimate type fixes that DID stick: function param types in `editorSvcDiscussions` setup helpers, `Record<string, any>` shapes for the marker maps and class-applier registries, and an `as any` cast on the `cledit` import boundary.
- **`shims.d.ts` extended with clunderscore prototype shims** — clunderscore extends `Array` / `NodeList` / `HTMLCollection` / `Function` / `Object` prototypes with `cl_each` / `cl_map` / `cl_some` / `cl_filter` / `cl_reduce` / `cl_bind` / `cl_extend` (used pervasively across cledit and the older services). Added ambient global interface augmentations so callers in `.ts` files can call those methods without per-callsite `as any` casts. The file remains an ambient script (no `export {}`) — that's a deliberate choice; converting it to a module breaks the `declare module 'foo'` lines for `prismjs` / `markdown-it` / `file-saver` / etc. Documented inline.
- **End-to-end validation re-run after batch 6** — same comprehensive Playwright audit: welcome doc, command palette, editor input → preview, localStorage persistence, reload recovery. **20/20 checks pass** with zero page errors and zero severe console errors.
- **TypeScript migration batch 5 — 5 mid-sized services ported (markdown grammar, file import, editor utils)** — `markdownGrammarSvc.js → .ts` (431 lines, recursive Prism grammar tree builder for the in-editor markdown highlighter; `GrammarOptions` interface for the entry-point options surface, the recursive grammar-token graph stays loose `Record<string, any>` since strict typing it would be busy-work for no payoff), `fileImportSvc.js → .ts` (166 lines, drag-and-drop import with `FileSystemEntry` folder traversal — added an `FsEntry` interface for `webkitGetAsEntry()` results since browsers don't ship a unified type), `editor/cledit/cleditUtils.js → .ts` (129 lines, cledit's `Utils` namespace — `defer` / `debounce` / `createEventHooks` / `findContainer` — typed via a `UtilsShape` interface), `editor/sectionUtils.js → .ts` (114 lines, scroll-sync section-dimension measurement — `SectionDimension` class + `SectionDesc` / `EditorSvcLike` interfaces), `editor/editorSvcUtils.js → .ts` (151 lines, scroll-position / preview-offset / scroll-to-anchor methods that get mixed onto `editorSvc` — `this` typed as `any` since `editorSvc` is huge and cross-cutting). Added `turndown/lib/turndown.browser.umd` to `shims.d.ts`. **21 services on TypeScript now** (3 + 5 + 4 + 4 + 5); ~43 still on JS — over 30% of services typed. Lint clean, typecheck clean, 361/361 unit tests pass, vite build clean.
- **End-to-end validation across all TS batches** — beyond unit tests, ran a comprehensive Playwright audit covering paths through every ported service: welcome-doc renders all default-enabled markdown extensions correctly (markdownConversionSvc / markdownGrammarSvc / extensionSvc / sectionUtils / shims), command palette opens via `Cmd+K` (Modal + customToolbarButtons + store), typing into the editor flows through cledit → converter pipeline → preview update, file id + open-folder state survive page reload via uiPersistence + localStorage, typed editor content survives reload (full workspaceSvc + localDbSvc chain). 20/20 checks pass with zero page errors and zero severe console errors.
- **TypeScript migration batch 4 — 4 large central services ported** — `markdownConversionSvc.js → .ts` (273 lines, the markdown-it converter / section-parser / Prism-highlight pipeline; `ParsingCtx` and `ConversionCtx` interfaces, internal markdown-it state and tokens cast to `any` since markdown-it's exported types are widened in `shims.d.ts`), `workspaceSvc.js → .ts` (312 lines, file/folder CRUD and path-uniqueness; `Item`, `CreateFileInput`, `Location` interfaces — adopted by callers like `explorerSvc` / `publishSvc` instead of the prior `as any` casts), `networkSvc.js → .ts` (345 lines, OAuth2 + offline detection + XHR with retry/backoff; `RequestConfig`, `RequestResult`, `OAuth2Params`, `OAuth2Result` interfaces), `animationSvc.js → .ts` (247 lines, requestAnimationFrame-driven element animator — converted the 3 vintage constructor functions `ElementAttribute` / `StyleAttribute` / `TransformAttribute` to proper TypeScript classes implementing a shared `AttributeHandler` interface, `EasingFn` interface for the bezier-easing v2-API shim). Added `prismjs` + `markdown-it` to `shims.d.ts` so the conversion pipeline imports type-resolve. Tightened earlier `as any` casts: `tempFileSvc` and `backupSvc` now pass typed values into `workspaceSvc.createFile` instead of widening at the boundary. **16 services on TypeScript now** (3 + 5 + 4 + 4); ~48 still on JS — crossing the 25% mark on services typed. Lint clean, typecheck clean, 361/361 unit tests pass, vite build clean. Live Playwright integration check on the welcome doc: all default-enabled markdown extensions render correctly with zero console / page errors.
- **TypeScript migration batch 3 — 4 medium services ported + ambient shims** — `exportSvc.js → .ts` (131 lines, file export pipeline; introduced `Heading` and `Template` interfaces, `markdownConversionSvc` calls cast to `any` until that file is ported), `publishSvc.js → .ts` (147 lines, publish location flow; `PublishLocation` interface for the wire shape, `properties` cast to `any` since `utils.computeProperties` returns a narrowly-inferred type from JS), `explorerSvc.js → .ts` (177 lines, file/folder delete flow; `ExplorerNode` interface for the tree wrapper that explorer/nodeStructure produces — only the fields actually touched here), `gitWorkspaceSvc.js → .ts` (235 lines, git tree → workspace change-set diffing; `TreeEntry`, `Change`, `ItemLike`, `SyncDataLike` interfaces). New `src/types/shims.d.ts` covers Vite's `?worker` / `?raw` / `?url` import suffixes plus ambient module declarations for untyped legacy deps (`file-saver`, `bezier-easing`, `diff-match-patch`, `handlebars`, `js-yaml`, `tinykeys`, `abcjs`, `mermaid`). `tsconfig.json` `include` extended to pick up `src/types/**/*.d.ts`. 12 services on TypeScript now (3 + 5 + 4); ~52 still on JS. Lint clean, typecheck clean, 255/255 unit tests pass, vite build clean. Live Playwright integration check on the welcome doc renders all default-enabled markdown extensions correctly with zero console / page errors.
- **TypeScript migration batch 2 — 5 more leaf services ported** — `draftFilesSvc.js → .ts` (34 lines, draft-tracking Map), `backupSvc.js → .ts` (74 lines, JSON backup importer with v4/v5 schema discrimination via a `BackupEntry` union), `userSvc.js → .ts` (91 lines, prefix-keyed user info resolver registry with a `UserInfo` interface), `tempFileSvc.js → .ts` (99 lines, embedded-mode postMessage IPC; cledit + editorSvc imports widened to `any` at the boundary because their JS surfaces are too large to type incrementally without porting them too — flagged for follow-up), `uiPersistence.js → .ts` (102 lines, localStorage UI-state persistence with typed `MutationLike` shape for the `store.subscribe` callback). 8 services on TypeScript now (3 from batch 1 + 5 from this batch). Vue components and the rest of `src/` stay JS — `allowJs: true` + `checkJs: false` keeps the migration incremental. Lint clean, typecheck clean, 255/255 unit tests pass, vite build clean. Live Playwright integration check against the welcome doc post-port: all default-enabled markdown extensions render (tables, task lists, footnotes, mark/strike/sub/sup, image dimensions, emoji, KaTeX, mermaid, deflists, abbreviations) — zero console / page errors.
- **markdown-it bumped 8.4.x → 14.1.x** — finally cleared the long-standing dep upgrade. Companion plugins also bumped: `markdown-it-abbr` 1 → 2, `markdown-it-deflist` 2 → 3, `markdown-it-emoji` 1 → 3, `markdown-it-footnote` 3 → 4, `markdown-it-mark` 2 → 4, `markdown-it-sub` / `markdown-it-sup` 1 → 2. Total adaptation surface ended up much smaller than the audit predicted: only **3 changes** across the whole codebase. (1) `markdownExtension.js`: renamed inline post-process rule `text_collapse` → `fragments_join` (markdown-it 14 renamed the rule but kept identical behavior — folds adjacent text fragments after delimiter resolution); also added `text_join` to the core rules array (new in 14, required for escape sequences like `\*` to render correctly). (2) `emojiExtension.js`: import switched from default `import x from 'markdown-it-emoji'` to named `import { full as x } from 'markdown-it-emoji'` — v3 split the export into `bare` / `light` / `full` variants, and `full` matches the v1 default behavior. (3) None of the four in-tree markdown-it plugins (`markdownItImsize`, `markdownItTasklist`, `markdownItMath`, `markdownItAnchor`) needed any code change — `md.helpers.parseLink*`, `state.md.inline.State`, `state.push`, `state.md.normalizeLink`, `state.md.validateLink`, `md.utils.normalizeReference`, `md.core.ruler.{after,before,push}`, `md.inline.ruler.{before,push}`, token `attrPush` / `attrIndex` are all stable across the major version bump. End-to-end Playwright validation on the welcome doc: 37 headings (all with anchor IDs), 6-row table, 4 task checkboxes, 4 footnote refs + footnote section, mark / strike / sub / sup, image with `=360x180` rendering at 360×180px, emoji 🚀, 2 KaTeX, 2 mermaid SVGs, definition list, 8 abbreviations, smart quotes — zero console / page errors.
- **Text-statistics popup expanded with markdown-aware metrics + new modal** — clicking the **i** toolbar button (or running it from the command palette) used to show a `window.alert()` with chars / words / lines / sentences / reading-time. Now opens a properly-sized in-app modal with three sections — **Counts** (chars / chars-no-whitespace / words / lines / paragraphs / sentences), **Structure** (headings with H1–H6 level breakdown / code blocks + inline / links + images / GFM task completion %), and **Reading** (reading time at 220 wpm / speaking time at 150 wpm / estimated LLM tokens at chars÷4). Markdown counts run on a code-stripped copy of the source (fenced blocks + inline backticks removed) so tutorial markdown that quotes `![alt](url)` inside backticks doesn't get double-counted. Optional sections only render when the doc contains the relevant construct, so plain prose stays terse. Modal grows to fit content — no scrolling on typical desktop viewports for documents up to ~50 stat-rows worth of structure. New `textStats` entry in `simpleModals.js` + `.text-stats__*` BEM-style CSS in `Modal.vue` (label/value rows with `font-variant-numeric: tabular-nums` so digit columns line up).
- **TypeScript scaffolding extended from `api/` into `src/services/`** — `tsconfig.json` `include` now covers `src/services/**/*.ts` alongside `api/**/*.ts`. Compiler set to `allowJs: true` + `checkJs: false` so `.ts` files can import `.js` neighbors without a whole-tree rewrite (only `.ts` files are type-checked, `.js` is reachable but treated as untyped). First three small leaf services ported as the migration template: `extensionSvc.js → .ts` (40 lines, pub/sub registry for the markdown converter pipeline; emits proper `GetOptions`/`InitConverter`/`SectionPreview` listener types), `timeSvc.js → .ts` (174 lines, `RelativeTime` class + `strftime` formatter, pure date utilities, no internal deps), and `badgeSvc.js → .ts` (37 lines, debounced badge-earned notification service with a minimal `Badge` interface). CI step renamed `Typecheck (api/)` → `Typecheck (api/ + services/)` to reflect the expanded scope. Establishes the pattern (one `.ts` per service, narrow types where obvious, `unknown`/`any` where the runtime surface is dynamic) without committing to a full whole-tree port up front. Vite's esbuild transform handles the new `.ts` files at build time — no other config changes needed. Verified: `npm run typecheck` clean, `npm run lint` clean, 247/247 unit tests pass, `npm run build` produces the expected chunks.
- **markdown-it 14 upgrade path clarified** — earlier notes claimed the in-tree pagedown port held back the markdown-it 14 upgrade. That's wrong: `src/libs/pagedown.js` is the text-editor library (chunks/selection/commands) and doesn't import markdown-it. The actual blockers for the markdown-it 14 upgrade are the in-tree markdown-it plugins that depend on internal-API surfaces that changed across major versions (`src/libs/markdownItImsize.js`, `src/extensions/libs/markdownItTasklist.js`, `src/extensions/libs/markdownItMath.js`, `src/extensions/libs/markdownItAnchor.js`). Pagedown modernization remains a worthwhile code-quality refactor but is independent of the markdown-it upgrade path. (Local `.claude/CLAUDE.md` framework note also updated; the file is git-excluded per `.git/info/exclude` so the correction is recorded here for the public record.)
- **Inline images in the editor default to off** — `defaultSettings.yml` `editor.inlineImages` flipped from `true` to `false`. The editor pane now shows raw `![alt](url)` markdown without the rendered image decoration alongside; the preview pane still renders normally. Reduces visual noise in image-heavy docs and matches the source-of-truth role of the editor pane. The setting is still per-user toggleable in Settings YAML — flip back to `true` to restore the legacy behavior. Affects new workspaces only; existing users keep whatever value is saved in their settings store.
- **Provider modals default to `main` instead of `master`** — all 8 GitHub / GitLab open / publish / save / workspace modals now suggest `main` as the implicit branch when the user leaves the field empty. Hint text updated to "If not supplied, the `main` branch will be used (use `master` for older repos)." `githubWorkspaceProvider.js` + `gitlabWorkspaceProvider.js` workspace bootstrap also defaults to `main`. Aligns with GitHub / GitLab's modern default for new repos. Users with legacy `master`-default repos can still type `master` explicitly.
- **CONTRIBUTING.md lint section refreshed** — the line claiming "Lint is not run in CI" was stale (CI does run `npm run lint` and fails on errors, as we relearned via PR #37). Replaced with a description of the actual ESLint 9 flat-config setup and how warnings vs errors are treated.
- **Music-notation button uses selection as the piece title** — selecting "Twinkle, Twinkle Little Star" then clicking the music button produces a block with `T:Twinkle, Twinkle Little Star` instead of `T:Untitled`. Multi-line selections collapse to a single space-separated title.
- **Mermaid button uses selection as a node label** — selecting text then clicking the mermaid button produces `flowchart LR\n  A["selected text"]` (single-node flowchart) instead of the empty A→B stub. Quotes escaped, multi-line selections collapse to a single space-separated label.
- **Block-insert buttons consume selection** — math / mermaid / music / horizontal-rule previously inserted at the cursor and left any selected range untouched. They now replace the selection (consistent with the wrap-style buttons), and the new `padForBlock` helper handles surrounding-newlines math at both ends of the range rather than just at the cursor.
- **Magic-wand button visually separated from insert-style buttons** — small 1 px vertical divider added before the tidy button (last in the custom toolbar group) since it's a tooling action on the whole document, not a content-insertion button. Driven by a `separated: true` flag on the button data; the toolbar template renders a `.navigation-bar__divider` before any button that carries the flag.
- **Focus-mode tooltip describes the actual behavior** — `Toggle focus mode` → `Toggle typewriter scrolling (keeps caret centered)`. The bullseye icon implied a distraction-free / dim-other-paragraphs feature, but the underlying flag (`layoutSettings.focusMode`) only flips `getCursorFocusRatio()` from `0.15` → `1`, which makes the cursor stay glued to the vertical center while you type (i.e. typewriter scrolling). Tooltip now matches what the click does.
- **Template dropdowns show short descriptions** — each of the 5 bundled templates (`Plain text`, `Plain HTML`, `Styled HTML`, `Styled HTML with TOC`, `Jekyll site`) gained a one-line `description` field surfaced inline in option text via middle-dot (e.g. "Styled HTML with TOC · Full document with TOC sidebar"). Same separator the nav-bar file-meta strip already uses (`size · words · lines …`). Applied to all 12 modals that render the template select (HtmlExport, PdfExport, Templates, plus 9 provider publish modals: Dropbox, Wordpress, Blogger, Blogger pages, Gitlab, Github, Zendesk, Gist, Google Drive). User-created custom templates have no description and render unchanged.

### Fixed
- **`options.{fence,table,del}` actually disable their rules now** — `markdownExtension.js` tried to disable optional markdown-it rules by splicing them out of the array passed to `markdown.{block,inline}.ruler.enable(...)`. Two latent bugs: (1) `enable(rules)` only turns rules ON — it doesn't turn off rules already enabled by the default preset — so the splice was a no-op even when correct. (2) The strikethrough splice used `blockRules.indexOf('strikethrough')` (returns -1 → `splice(-1, 1)` drops the LAST entry of `inlineRules`/`inlineRules2` instead, namely `entity` and `fragments_join`); the second bug was hidden because the first one stopped the splice from mattering. Switched the opt-out path to `markdown.block.ruler.disable('fence')` / `disable('table')` and `markdown.inline.ruler{,2}.disable('strikethrough')`, which actually flips the rule's enabled flag. Net effect: file properties that set `extensions.markdown.fence: false`, `table: false`, or `del: false` now work as documented. Added 8 regression specs in `test/unit/hardening/markdownItPlugins.spec.js` exercising both directions (rule on / off) — they fail if a future change reintroduces the splice-and-enable pattern. Test count 247 → 255.
- **Image-with-dimensions toolbar button now actually applies the dimensions** — the **Image with dimensions** button at `customToolbarButtons.js:458` was emitting `![alt](url){width=300}` (Hugo / kramdown / pandoc attribute syntax) on the false premise that "markdown-it-imsize isn't bundled" — but it IS bundled and active. The bundled extension only recognizes `=WIDTHxHEIGHT` (with a space before `=`); the `{width=…}` was passed through as literal text after the rendered image, with no width applied. Switched the emitted snippet to `![alt](url =300x)` (width-only, height auto-derived from the image's aspect ratio) and corrected the inline comment. Verified end-to-end: rendered output is `<img src="..." alt="alt text" width="300">` with no trailing text. Hugo / kramdown / pandoc export targets lose the explicit width and fall back to plain `<img>` — a reasonable trade since the in-app preview is the higher-traffic surface.
- **Cmd+K command palette didn't open** (post-wave-4 regression — fixed) — `CommandPaletteModal` was created but never registered in `Modal.vue` because the relevant Edit calls silently no-oped on a "File has not been read yet" guard. Registration added; end-to-end Playwright now verifies palette opens, input focuses, typing filters, Escape closes.
- **Magic-wand button vanished after click** — `<template v-for>` with mixed `<div>` (divider) and `<button>` siblings interacted poorly with the parent's float-based layout, pushing the wand off-row after the first paint. After several rounds of refinement, the divider element was replaced entirely with a `::before` pseudo-element on the button itself (`navigation-bar__button--separated` class). Costs only an 8 px left margin and adds no element to the float context, so the wand stays put across viewport widths. Pseudo's alpha bumped 0.22 → 0.4 so the line is actually visible against the dark navbar.
- **Folder / toolbar buttons stayed darkened after click** — the generic `.button` class in `app.scss` paired `background-color: rgba(0, 0, 0, 0.05)` with `:focus`, so clicking the explorer-toggler folder, modal buttons, and other top-bar buttons left them visually stuck-highlighted until the user clicked elsewhere. Switched the rule to `:focus-visible` (both light and dark variants) — keyboard tab nav still gets the affordance, mouse click no longer sticks. Same root cause as the earlier nav-bar-button fix; this is the generic-class equivalent.
- **Toolbar buttons stayed highlighted after click** — clicking a navigation-bar button (undo, formatter buttons, the new tidy magic-wand, etc.) left the button visually highlighted because `.navigation-bar__button { &:focus { background-color: hover-bg } }` paired the same bg with focus as with hover. After click, focus stayed on the button and the bg stuck. Same fix as the modal-tab regression — switched to `:focus-visible`. Keyboard tab nav still shows the affordance.
- **Block-insert toolbar buttons added too much whitespace** — `insertBlock` (used by horizontal-rule, math, mermaid, music) always appended a fixed `\n\n` regardless of what was already after the cursor, so inserting before existing blank-line-separated content compounded to triple newlines (= two blank lines). Tightened to count existing newlines on each side and pad with exactly the missing newlines for one blank line above + below. EOF gets a single trailing newline instead of two.
- **Permanent horizontal scrollbar in the editor on macOS** — `.editor` used `overflow: auto`, which on macOS with "Always show scrollbars" enabled rendered a horizontal scrollbar even though the `<pre class="editor__inner">` uses `white-space: pre-wrap; word-break: break-word` and never overflows. Fenced code blocks already have their own `overflow-x: auto`. Switched to `overflow-x: hidden; overflow-y: auto` so the editor stays vertical-scroll only.
- **Empty-strip flash on explorer toggle** — closing the file explorer used to flash a brief blank strip on the right side of the editor for ~220 ms. Cause: the `width 220ms cubic-bezier` transition added in v5.16.0 for the right side-bar's TOC mode-switch was applied to `.layout__panel` broadly, so the editor / preview panels' new wider container animated open faster than the editor's internal CodeMirror content reflowed. Scoped the transition to `.layout__panel--side-bar` only — explorer toggle is now instant again, side-bar TOC mode-switch keeps its smooth slide.
- **Lint warnings to zero** — disabled the `vue/multiline-html-element-content-newline` cosmetic rule (was firing on the search-clear `×` button and the `ExplorerNode` row template; both intentionally on a single line for layout reasons). Removed an unused `eslint-disable-line no-continue` directive in `explorerSvc.js` that the lint config doesn't actually flag.
- **File Properties modal opens cleanly on YAML tab** — the Simple-properties tab no longer visually masquerades as the active one after modal open. Root cause: `.tabs__tab > a` painted the same grey background on `:focus` as on `:hover`, so any programmatic / sticky focus on the inactive tab made it look selected against the actual YAML tab's blue underline. Tightened the rule to `:focus-visible` — keyboard nav still gets the bg affordance, mouse clicks and modal auto-focus do not.

## [5.16.0] - 2026-04-25

First release as an independent successor to `benweet/stackedit` (dormant since 2023-05-27). Drops the `-fork.N` suffix in favor of continuing the StackEdit lineage where upstream stopped. Version `6.x` is reserved for the eventual rebrand (new logo + name); until then this codebase continues `5.x` semver.

### Changed
- **About modal reworded for independence** — pill badge `Community Fork` → `Independent Successor`. Top links retitled `Fork on GitHub` → `View on GitHub`, `Fork changelog` → `Changelog`. The standalone "Upstream StackEdit — Upstream community" line is gone (the community forum is also dormant); replaced with a single demoted "Based on benweet/stackedit (dormant since 2023)" line tucked under the version, attributing the codebase origin without sending users to a graveyard. Added a one-line tagline ("Markdown editor with live preview, Mermaid, KaTeX, and cloud sync. Actively maintained continuation of StackEdit.") between the badge and the GitHub links so the modal opens with a clear "this is what the project is" line. Copyright `Fork © 2026 soreavis` → `© 2026 soreavis`. The Dock5 © 2013–2019 line and the Apache-2.0 link stay verbatim per license obligations.
- **Project status: independent successor.** Upstream `benweet/stackedit` has been dormant since 2023-05-27 (last commit; last GitHub release 2019-07-02). The "stay merge-compatible with upstream" constraint that previously gated refactors is retired. README + CONTRIBUTING + SECURITY repositioned from "fork tracking upstream" to "actively-maintained successor"; Apache-2.0 attribution to Benoit Schweblin (LICENSE, NOTICE, About modal copyright line) preserved verbatim per license obligations. `.claude/CLAUDE.md` updated: framework migrations (Vue 3 / Pinia / TypeScript / CodeMirror 6) and file-layout restructures are no longer scope-blocked.
- **Default branch renamed `master` → `main`.** GitHub default branch swapped, branch protection rules transferred (PR required, no force-push, no deletion). Refs updated in `.github/workflows/ci.yml` (push + PR triggers), `package.json` `postversion` script, in-app links (`AboutModal.vue` "Fork changelog", `FilePropertiesModal.vue` "see here" presets link, repointed to our own repo since upstream is dormant), `PRIVACY.md` + `static/privacy.html` changelog link. Released-section CHANGELOG history left unmodified per Keep-a-Changelog. Provider-modal placeholders that show "master" as the user's-external-repo default are unchanged — those refer to the user's own GitHub/GitLab repo defaults, not ours.

### Added
- **Enter = confirm in every modal** — Modal root listens for keydown.enter and forwards to `.button--resolve` unless the target is a `TEXTAREA` or contenteditable. No per-modal wiring needed; covers simpleModals, FilePropertiesModal, export modals, etc.
- **Right-click menu on the nav-bar filename** — contextual menu with Rename / File properties / Copy path / Close file, using the existing `contextMenu/open` store action.

### Changed
- **Folder expand/collapse animates** — explorer folder open/close now eases via Vue `<transition>` + JS hooks driving `style.height` from 0 ↔ `scrollHeight`. 200 ms `cubic-bezier(0.4, 0, 0.2, 1)` (Material standard easing, same curve as the side-bar width transition). Nested folders animate independently because each `ExplorerNode` wraps its own `.explorer-node__children`. `prefers-reduced-motion: reduce` strips the transition for instant toggle. CSS rule scoped to `.explorer-folder-enter-active` / `-leave-active` so unrelated height changes (rename, search filter) don't pick it up.
- **File Properties dialog opens on YAML tab by default** — `defaultLocalSettings.filePropertiesTab` flipped from `''` to `'yaml'`, and `FilePropertiesModal.vue` `created()` only forces the Simple tab when `tab === 'simple'` (legacy `''` and any other value land on YAML). Existing users who never explicitly chose Simple now get YAML on first open. Anyone who explicitly selected Simple keeps Simple.
- **Explorer expand-all / collapse-all buttons use chevron icons** — replaces the unicode `⊞` / `⊟` glyphs in the explorer toolbar with `<icon-chevron-down>` (expand-all, folders fan open) and `<icon-chevron-up>` (collapse-all, folders roll up). Reads more cleanly at the toolbar's icon size and matches the rest of the SVG-based toolbar buttons. New `src/icons/ChevronUp.vue` + `ChevronDown.vue` use a tightened `viewBox="4 4 16 16"` (cropping the 24×24 empty margin) so the chevron renders ~1.5× larger and balances visually with the surrounding fill icons. Registered globally as `iconChevronUp` / `iconChevronDown`. Drops the now-unused `.side-title__tree-glyph` CSS rule.
- **Sort-mode glyph alignment** — the `◷` clock (recently-opened) gets `transform: scale(1.3)` so its rendered footprint matches the chevrons without growing the line box (a font-size bump pushed the glyph below the optical center). `A↓` (name) and `✱` (created) get `position: relative; top: 1px` to drop them onto the same baseline as the chevrons / X icon. Each sort mode now tags its span with `side-title__sort-glyph--<mode>` so the rules stay scoped.
- **Status-bar side labels are icons** — the literal `Markdown` / `HTML` words in `StatusBar.vue` are replaced with `<icon-language-markdown>` (boxed `M↓`, MDI's standard markdown logo) on the left block and `<icon-language-html5>` (HTML5 shield) on the right. Sized 14×14 with `vertical-align: -2px` to sit on the numeric-stat baseline; `v-title` tooltips ("Markdown source" / "Rendered HTML") preserve discoverability. New `src/icons/LanguageMarkdown.vue` + `LanguageHtml5.vue` registered globally.
- **Mermaid diagrams centered in HTML export** — the `text-align: center` rule previously scoped to `.stackedit--pdf` in `styledHtmlTemplate.html` / `styledHtmlWithTocTemplate.html` now also applies to plain HTML export (under `.stackedit`). Inline SVG also gets `display: inline-block; max-width: 100%` so it respects the centered parent and doesn't overflow narrow containers. PDF-only page-break rules stay in the `{{#if pdf}}` guard.
- **HTML-with-TOC export widens the content column** — `.stackedit__html` `max-width` bumped 750 → 1050 px (+40%) inside `.stackedit__right`, scoped to `styledHtmlWithTocTemplate.html` only and gated by the same `min-width: 1060px` breakpoint where the 250 px TOC sidebar actually docks. No-TOC export unchanged.
- **Scrollspy in HTML-with-TOC export** — the current heading's TOC link picks up `.toc-active` (brand blue, semi-bold, subtle tinted bg, 120 ms transition) as the reader scrolls. Small vanilla-JS loop picks the last heading whose doc-top is within `scroll + 120 px` and auto-scrolls the `.stackedit__left` pane when the active link goes out of view. **Only tracks headings that have a matching TOC link** — the `tocToHtml` helper is depth-limited (the styled template passes `2`, so only h1/h2 render), but documents often contain deeper h3/h4/h5/h6 with ids; tracking every heading would clear the highlight whenever the reader scrolled into a sub-heading and leave the pane blank until the next top-level heading. Verified against a 383-heading real document. Wrapped in `{{#unless pdf}}` so PDF export stays script-free. Also adds `html { scroll-behavior: smooth }`.
- **In-app TOC side-bar widened +40%** — when `layoutSettings.sideBarPanel === 'toc'`, the side-bar panel renders at 392 px (up from 280 px) so the table of contents can breathe. All other side-bar panels (Menu, Sync, Publish, History, Export, Import/Export, Workspaces, Help) stay at 280 px. Driven by a new `styles.sideBarWidth` getter in `src/store/layout.js` plus a `Layout.vue` binding switch. `.layout__panel` picks up a `width 220ms cubic-bezier(0.4, 0, 0.2, 1)` transition so the side-bar, editor, and preview slide in lockstep when the TOC panel opens/closes; `prefers-reduced-motion: reduce` disables the transition.
- **TOC headings get a hover affordance** — individual `h1…h6` entries under `.cl-toc-section` darken (`rgba(0, 0, 0, 0.95)`) and pick up a subtle `rgba(0, 0, 0, 0.06)` background on hover, with a 120 ms color/bg transition. Required removing `pointer-events: none` from `.toc__inner *` so children receive hover; the drag-to-scroll mousedown/mousemove handlers still fire via bubbling on `.toc__inner`. Vertical `margin` on each heading was converted to `padding-top/-bottom: 0.4rem` (and indentation moved from `margin-left` to `padding-left`), so the hover target covers the full row gutter instead of just the text-bbox and the highlight bg extends flush-left.
- **Recent folder ordering frozen per session** — opening a file bumps its `data/lastOpened` timestamp, which previously re-sorted the Recent folder on every click. `uiPersistence.restoreCurrentFile` now seeds `explorer.recentSnapshot` once at boot; `nodeStructure` prefers the snapshot. Reload reseeds. Relative-time labels ("5m", "2h") still age live.
- **Mermaid spacing**: `rankSpacing: 30` (down from library default 50) tightens vertical gaps in stacked TB flowcharts without spanning screens. Paired with `subGraphTitleMargin: { top: 20, bottom: 20 }` to keep subgraph titles clear of their first child. Other flowchart knobs stay at mermaid defaults — every combination we tried beyond these two triggered mermaid 11's edge-label bbox bug (empty "pill" where the label should be).
- **Mermaid action-icons enlarged** earlier in the 5.15.5-fork.2 release (28 → 35 px) stay.

### Fixed
- **Edge labels render correctly again** — wholesale revert of the earlier five-knob spacing override (padding / nodeSpacing / rankSpacing / diagramPadding / subGraphTitleMargin), replaced with the minimal two-knob combo above. "framework forbids same" and "PR review time grows" both draw text instead of empty pills.
- **Empty pages in PDF export** — the aggressive `page-break-after: avoid` on every heading + `page-break-before: avoid` on every mermaid wrapper was forcing Chromium to leave huge blank slabs when a heading + diagram couldn't fit on the current page. Relaxed to just `page-break-inside: avoid` on mermaid wrappers; diagrams still don't split mid-box, headings can break where they naturally fall.

## [5.15.5-fork.2] - 2026-04-24

Explorer multi-select + search, PDF mermaid rendering + fit-to-page, top-bar file metadata, empty-state placeholder, close-file + draft auto-discard, general mermaid polish, plus explorer power-features (sort, pinning, Recent folder, keyboard nav, drag-out export, per-row nerd-info, reload persistence).

### Added
- **Folder file-count badges** — every folder gets a recursive descendant-file count pill. Hidden when zero; root excluded. Badge flips to white on the blue-selected row.
- **Expand-all / Collapse-all toolbar buttons** (⊞ / ⊟ glyphs) — iterate `folder/items` and set `openNodes` wholesale via a new `setOpenNodes` mutation.
- **Sort toggle in the explorer toolbar** cycles name → modified → created; persisted in `localSettings.explorerSort`. `nodeStructure` uses a comparator that also places **pinned folders** first. Pin/Unpin action in the context menu; 📌 glyph next to pinned folder names; pinned ids persisted in `localSettings.pinnedFolderIds`.
- **Context menu additions** on explorer nodes: Duplicate (files), Reveal in editor (files), Copy path (any real node), Pin/Unpin folder (regular folders only).
- **Selection-aware toolbar** — new-file / new-folder / rename hide when more than one node is selected; delete stays active since it handles multi.
- **Synthesized "Recent" folder** at the top of the explorer tree — lists the 10 most recently-opened non-trash files by `data/lastOpened` timestamp; entries are clones linked by `item.id` so clicks open the real file. Shows a relative timestamp next to each row ("now", "5m", "2h", "3d", "Apr 12").
- **Keyboard navigation** on `.explorer__tree`: ArrowUp / ArrowDown moves primary selection across visible nodes (Shift extends range using the visible-DOM anchor). Enter opens a file or toggles a folder. F2 renames. Cmd/Ctrl+D duplicates the current file via `workspaceSvc` + `localDbSvc`. Delete keeps the existing handler. Auto-scrolls the new primary into view.
- **Drag-out-to-OS export** — when dragging a file whose content is already in `state.content.itemsById` (i.e. the currently-open file or one opened this session), `onDragStart` also sets the HTML5 `DownloadURL` + `text/plain` payloads. Dropping onto Finder / the desktop produces a real `.md` file. `onDragEnd` revokes the blob URL. No-op for folders, multi-selection drags, and unloaded files — internal drag semantics unchanged.
- **HTML drag-drop import** — `fileImportSvc` now accepts `.html` / `.htm` / `.xhtml` alongside markdown. HTML files are sanitized with DOMPurify and converted via Turndown before being stored. The workspace only ever contains `.md`, same as before.
- **Disabled-state affordances** — `MenuEntry` gained a `disabled` prop (dimmed opacity, `cursor: not-allowed`, click blocked at `@click.capture`). Applied to Sync / Publish / History / File properties / Print in the main menu and all four export entries (Markdown / HTML / PDF / Pandoc) in the Import/Export panel when no file is current. In the Explorer toolbar: trash disabled unless a real (non-sentinel) node is selected; rename disabled unless the primary selection is a real node.
- **Discoverable tooltips** on NavigationBar icons — dynamic "Click to open file panel" / "Click to close file panel" on the folder toggle; "Click to open menu" on the StackEdit logo.
- **Per-row nerd-info** ⓘ glyph on the primary-selected explorer row. Custom popover (not native title) with uppercase/letter-spaced keys and full values (name, path, type, id, for loaded files: size / words / lines / reading time @ 220 wpm, for folders: descendant count, plus last-opened timestamp). Positioned fixed at the icon's bottom-left; pointer-events off so hover doesn't bounce; icon itself vertically centered via `position: absolute; top: 50%; translateY(-50%)`.
- **Footer nerd stats** — `StatusBar` gained `chars`, `sentences`, `headings`, `code`, `links`, and `read` (reading time @ 220 wpm) on the Markdown side; `sentences` and `read` on the HTML side. The `Stat` class now supports either a regex counter or a custom `computeFn(text) → value` for derived stats like reading time.
- **UI persistence across reload** — new `services/uiPersistence.js` subscribes to mutations and mirrors `explorer.openNodes` + `file/setCurrentId` into `localStorage` (`stackedit.ui.openNodes`, `stackedit.ui.currentId`). `App.vue` restores open folders early (before `syncSvc.init`) and the current file id after. Reloading lands the user on the exact same file with the same folders expanded instead of defaulting to whichever file was "most recent".
- **Explorer multi-select** (Cmd/Ctrl-click to toggle, Shift-click for range in visible order, drag a marquee on empty tree space; hold a modifier during marquee to add to the current selection). Multi-drag moves every selected node in one gesture, drop-to-trash / drop-to-folder / drop-to-root all iterate the set, and a single aggregate `bulkDeletion` modal replaces the per-item prompts ("Move N items to Trash and permanently delete M items (including X folders with their contents). Are you sure?"). Descendants are pruned from the set so a folder's recursive delete doesn't double-process its own children, and the `setDragTarget` circularity check now rejects targets nested inside ANY dragged source.
- **Explorer search** — input below the toolbar filters the tree as you type. Matching files and their ancestor folders stay visible; folders containing a match auto-expand regardless of their user-toggle state; the matched substring is highlighted inline with a yellow pill (brighter yellow on black when the row is also the blue-selected primary). Esc clears. Pure local string match, no network. The input itself fades into the panel when idle (muted background, transparent border) and lights up on hover / focus with a soft blue focus-ring.
- **Clickable folder caret + first-click-selects UX**: regular folders now have a real `<span class="explorer-node__caret">` at the left edge. Clicking the caret toggles open/close independently of selection. Clicking the folder row itself *selects* the folder on first click and *toggles* only on a second click — previously any click both selected and toggled, which users found disorienting when they were trying to see which folder was currently focused before opening it. Trash/Temp sentinels keep the single-click-toggle behavior since they aren't part of multi-select.
- **Top-bar file metadata** — muted 11 px strip left of the filename shows `size · words · lines · reading time · parent path` for the current file, with a richer hover tooltip adding path, character count, reading-time estimate (@ 220 wpm), file id, and last-opened timestamp. Informational only, no storage or network.
- **"No file selected" empty state**: when no file is actively open (delete, close, or workspace-without-files) a centered placeholder card takes over the editor/preview area — icon, headline, hint, and a "New file" button that creates a blank file and switches to it. Subtle 45° striped backdrop; dark/light-theme aware.
- **Close-file button (× in the nav bar)**: deselects the current file and shows the empty state. A session flag (`explorer.userClosedFile`) prevents `localDbSvc`'s watcher from auto-reopening `file/lastOpened`. Opening any real file clears the flag. Session-scoped — a reload still re-opens the last-used file.
- **Auto-discard unedited new files**: when "New file" is triggered from the explorer's `+` button or the empty-state's "New file" button, the freshly-created file is tracked as a draft with a snapshot of its initial text. If the user switches away or closes the file without editing its content, `draftFilesSvc.discardIfUnedited` deletes it instead of leaving an empty `Untitled` stub behind. Imports / clipboard paste / drag-and-drop imports are not marked as drafts.
- **Mermaid in PDF + HTML exports** — previously the async mermaid render finished *after* `exportSvc.applyTemplate` had already serialized `containerElt.innerHTML`, so exports shipped with raw ` ```mermaid ` code fences instead of rendered SVG. `extensionSvc.sectionPreview` now returns `Promise.all` of listener results, mermaid's listener returns its in-flight render promise, and the export pipeline `await`s before reading the DOM. Live preview keeps its fire-and-forget path unchanged.
- **Fit-to-page diagrams in PDF export**: inline print-scoped CSS in the two bundled PDF templates (`styledHtml`, `styledHtmlWithToc`) caps every mermaid SVG to `max-width: 100%` / `max-height: 90vh` and scales down oversize diagrams instead of splitting them across pages. `page-break-inside: avoid` on the wrapper, `page-break-before: avoid` on the wrapper, `break-after: avoid` on all headings and on paragraphs that directly precede a mermaid block (via `:has()`) — so a caption/heading can't get orphaned at the bottom of a page with its diagram on the next one. An info note under the PDF-export modal documents the behavior and the template applicability.
- **Mermaid SVG export + lightbox buttons** increased to 35 × 35 px (was 28) with 18 px glyphs for better hit target. Interactive action buttons are stripped from HTML/PDF exports so diagrams don't ship with a "copy source / enlarge" toolbar baked into the image.

### Changed
- **Folder creation lands at root when the selected folder is collapsed**: clicking the new-folder button with a collapsed folder selected used to bury the new folder inside the closed branch. Now `explorerSvc.newItem` falls through to root instead. Same for new files, since the button shares the code path.
- **Mermaid diagrams are horizontally centered** inside the content column (live preview + HTML export + PDF export) via `text-align: center` on the wrapper + `display: inline-block` on the direct-child SVG.
- **Much more padding inside mermaid nodes and subgraphs** (bumped twice during the release): flowchart `padding` 8 → 32, `nodeSpacing` / `rankSpacing` 50 → 80, `diagramPadding` 8 → 24, `subGraphTitleMargin { top: 22, bottom: 22 }`. Subgraph titles and node labels no longer sit flush against their borders.
- **Explorer panel width 260 → 320 px** so the new toolbar (sort + expand-all + collapse-all) fits without cropping.
- **Sidebar back-to-main-menu icon** changed from the three-dots (`icon-dots-horizontal`) to a left arrow (`icon-arrow-left`), which reads much more clearly as a "back" affordance. Tooltip updated to "Back to main menu".
- **Drag-and-drop to workspace root**: the entire empty area of the file explorer is now a drop zone for "move to document root". Drag any folder or file onto the explorer background (or hover over the root itself during a drag) to move it back to the top level. Previously the only drop target for root was a barely-visible 20 px strip at the bottom of the root file list. A subtle blue inset border highlights the tree while root is the active drop target.
- **Recent folder uses the pseudo-element caret** (like Trash / Temp) and a single click toggles open/close. Earlier revision used the real-span caret which misfired because Recent is not in `nodeMap`.

### Fixed
- **Delete-last-file no longer pops open an unrelated collapsed folder**. Root cause was two-fold: `localDbSvc`'s own current-file watcher re-resurrected `file/lastOpened` on null, and `file/lastOpened` happily returned files buried inside collapsed folders (or just-trashed files), which then caused `openNode` to walk up and expand those folders. Fix: `file/lastOpened` now skips files whose ancestor chain contains a collapsed folder (via `rootState.explorer.openNodes`) *or* that live under Trash at any depth. `explorerSvc.deleteItem` (single + bulk paths) picks the next current via a new `pickVisibleReplacement()` that applies the same visibility test. `localDbSvc` only bootstraps a Welcome file when `file/items.length === 0` (truly-empty workspace), not whenever `lastOpened` returns empty.
- **About / Settings / File Properties modals broken when the empty-state was visible**. The `.layout__empty` overlay was using `z-index: 5`, which placed it above `.modal` (no z-index) in the root stacking context. Fix: moved the overlay after the editor/preview panels in DOM order and dropped the z-index, so it stacks above the editor by document order but below anything outside `<layout>`.
- **Blank row at the top of the explorer tree** — the synthetic root node was rendering its empty `__item` div, which became visible as soon as the new file-count badge got attached to it. Skipped via `v-else-if="!node.isRoot"` and `showFileCount` now also excludes root.

### Added (carried over — first published in this release cycle)
- **Mermaid lightbox background toggle**: new "☀ / ☾" button in the lightbox toolbar switches the full-viewport backdrop between the default dark wash and a pure-white surface that blends seamlessly with the diagram background. Useful for light-mode screenshots and presentations. The toolbar, close button, and hint strip keep their dark chrome in both modes so they stay readable either way.
- **Code block copy button**: every rendered fenced code block in the preview gets a hover-revealed "⧉" copy button in the top-right corner, mirroring the Mermaid diagram pattern. Click copies the raw source of the code block to the clipboard; the button flashes ✓ for 1.2 s on success. Mermaid diagram blocks are skipped (they already have their own copy-source button inside the diagram toolbar). Pure client-side, no network.
- **Drag-and-drop import**: drop `.md` / `.markdown` files (or whole folders of them) onto the file explorer panel to import them. Drop target determines the destination — drop on a folder lands inside it, drop on a file lands in its parent, drop on the empty area lands at the workspace root. Folder structure is mirrored; non-markdown files are silently skipped. Uses `FileReader` + `FileSystemEntry`, no network.
- **Mermaid diagram lightbox**: each rendered Mermaid diagram gets a hover-revealed "⤢ Enlarge" and "⧉ Copy source" button. Enlarge opens a fullscreen viewer that defaults to fit-to-viewport with drag-to-pan, scroll-to-zoom (cursor-anchored), and a toolbar for zoom in / zoom out / reset / copy source. Double-click the diagram to reset to fit. Esc or the × button closes. The copy button writes the original Mermaid source to the clipboard for pasting into external tools (mermaid.live, etc.).
- **Mermaid diagram export**: two new toolbar buttons in the lightbox — **SVG** downloads the diagram as a standalone `.svg` file (portable, self-contained — mermaid inlines fonts and styles); **PNG** rasterizes at 3× natural resolution for retina-ready screenshots. Flowchart `<foreignObject>` labels are swapped for plain SVG `<text>` in the PNG pipeline so the canvas isn't tainted. Pure client-side, no network.

## [5.15.5-fork.1] - 2026-04-17

First tagged release of the community fork. Cumulates the full hardening pass plus the first fork-branded UI.

### Added
- **Import from clipboard** menu entry in the Import/Export side-bar. Reads both `text/html` and `text/plain` clipboard flavors, prefers plain text when it already looks like Markdown (≥2 of: ATX heading, bullet, ordered list, code fence, bold, link, blockquote, hr, inline code), otherwise sanitizes + Turndowns the HTML. Auto-derives the file name from the first heading.
- `importClipboard` feature badge.
- Fork attribution in the About modal (version line + copyright + "Community Fork" badge + link rewrites to this repo).
- Vercel deployment: 4 Edge-runtime API routes (`conf`, `userInfo`, `cspReport`, `googleDriveAction`), 1 Node route (`githubToken`), 2 stubs (`pdfExport`, `pandocExport`).
- `/api/health` endpoint for uptime monitors.
- Upstash-backed distributed rate limiting with per-instance in-memory fallback.
- Paste-ready markdown test fixtures under `test/fixtures/` (KaTeX, Mermaid, sanitizer XSS vectors, YAML front-matter, API curl recipes).
- 202 Vitest specs under `test/unit/hardening/` covering sanitizer, rate limiter, API handlers, PKCE, template worker sandbox, `vercel.json` contract, and markdown-it plugins.
- `@vercel/analytics` + `@vercel/speed-insights` injection.
- `vite:preloadError` handler (skew protection for long-lived tabs).
- Custom `/404.html`, `/robots.txt`, root `/favicon.ico`.
- GitHub Actions CI (build + unit on every PR).
- Dependabot config (weekly npm + github-actions).

### Changed
- Vite 5 → Vite 6.4; Vitest 1 → Vitest 2.
- Vue aliased to runtime-only build (`vue/dist/vue.runtime.esm.js`) — the `parseHTML` ReDoS parser is now dead-code-eliminated from the shipped bundle.
- DOMPurify 3.4 replaces the forked Angular `$sanitize`.
- Trusted Types enforced (`require-trusted-types-for 'script'` + default policy); `'unsafe-eval'` scoped to the template-worker chunk only via per-file CSP.
- Tight CSP: enumerated `connect-src`, `frame-src` limited to `'self' https://accounts.google.com`, HSTS preload, COOP/CORP, expanded Permissions-Policy.
- OAuth: GitHub flow upgraded to PKCE S256.
- API routes: `sameOrigin` check, body-size caps, rate limits, masked error bodies.
- Build chunking: main bundle 2.8 MB → 595 KB (gz 828 KB → 154 KB). Mermaid is lazy-loaded.
- Sass modernized: `@import` → `@use`, `mix`/`lighten`/`darken`/`transparentize` → `color.adjust`, `/`-division → `math.div`. `normalize-scss` → `modern-normalize`.
- `js-yaml` 3 → 4, `handlebars` 4.0.10 → 4.7.8, `turndown` 4 → 7, `katex` 0.13 → 0.16, `mermaid` 8 → 11.
- Node engine pinned to `22.x`.
- Default branch remains `master` (matches upstream convention).

### Removed
- ~48 webpack-era devDeps (loaders, gulp, node-sass, stylelint, etc.) — Vite handles these.
- Unused runtime deps: `aws-sdk`, `request`, `body-parser`, `compression`, `serve-static`, `tmp`, `google-id-token-verifier`, `indexeddbshim`, `babel-runtime`.
- Upstream's Travis CI configuration (replaced with GitHub Actions).

### Fixed
- Scroll-sync: `animationSvc` calls re-wired for `bezier-easing@3` (library dropped the `.get()` / `.toCSS()` methods between v2 and v3). Preview pane again tracks editor scroll while the toggle is on.
- Keyboard shortcuts init: hoisted the `expansions` const above the `immediate: true` watcher in `shortcuts.js` to fix a TDZ `ReferenceError` at module load.

### Security
- DOMPurify replaces regex-based sanitizer; all `v-html` paths now route through it (including `CurrentDiscussion`, which was unsanitized upstream).
- `target="_blank"` anchors auto-get `rel="noopener noreferrer"` (36 anchors across 16 files).
- Dev server blocks `/api/*` source disclosure.
- Pandoc dev middleware validates metadata keys against `/^[A-Za-z0-9_-]{1,64}$/` and strips CR/LF from values.
- Source maps disabled in production builds.
- Third-party script loaders (Google `apis.google.com/js/api.js` offline check, landing-page `stackedit.io/style.css`) removed.
- AppCache manifest (deprecated) removed from landing page.

See [`feat(security)` commit](https://github.com/soreavis/stackedit/commit/4c87b6b) for the initial hardening pass; subsequent commits build on it.

## Prior history (upstream)

For changes before this fork diverged, see [upstream releases](https://github.com/benweet/stackedit/releases). Upstream's last published tag when this fork began was **v5.15.4** (Dec 2023).
